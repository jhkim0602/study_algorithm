// Ch07 문자열 검색 — 개념 정리 (CS 전공자 수준 세분화)
// 출력: concept = { summary, blocks }
// 허용 viz: StringSearchVisualizer, BoyerMooreViz, BFvsKMPViz

export const concept = {
  summary:
    '문자열 검색(string searching)은 길이 n의 텍스트(text) 안에서 길이 m의 패턴(pattern)이 ' +
    '나타나는 위치(또는 존재 여부)를 찾는 문제다. 모든 알고리즘의 본질은 패턴을 텍스트 위에 ' +
    '"맞춰 보고(정렬), 비교하고, 불일치하면 옮기는" 반복이며, 알고리즘들의 진짜 차이는 ' +
    '<b>불일치가 났을 때 패턴을 얼마나 똑똑하게(몇 칸) 이동시키는가</b>에 있다. ' +
    '브루트 포스는 비교 정보를 버리고 한 칸씩만 옮겨 O(nm)이고, KMP는 패턴 내부의 ' +
    '접두사=접미사 정보(실패 함수/skip table)를 재사용해 텍스트 포인터를 되돌리지 않고 ' +
    'O(n+m)에 끝낸다. 보이어-무어는 패턴을 끝에서부터 비교하고 불일치 문자에 따라 큰 폭으로 ' +
    '점프해 실무에서 평균적으로 가장 빠르다.',
  blocks: [
    // ──────────────────────────────────────────────
    { type: 'h3', text: '1. 문자열 검색이란 무엇인가' },

    { type: 'h4', text: '문제 정의: 텍스트, 패턴, n과 m' },
    {
      type: 'p',
      html:
        '<b>문자열 검색</b>은 어떤 긴 문자열 안에서 우리가 찾고자 하는 짧은 문자열이 ' +
        '나타나는 위치를 알아내는 문제다. 정렬이나 트리 생성, 연결 리스트 탐색 같은 것이 아니라, ' +
        '<b>문자열 안에서 특정 패턴을 찾는 과정</b> 그 자체가 정의다.',
    },
    {
      type: 'list',
      items: [
        '<b>텍스트(text)</b> — 검색의 <u>대상</u>이 되는 전체 문자열. 길이를 <code>n</code>으로 쓴다. (찾는 대상이 아니라 "뒤지는 무대").',
        '<b>패턴(pattern)</b> — 우리가 <u>찾고자 하는</u> 문자열. 길이를 <code>m</code>으로 쓴다. 보통 <code>m ≤ n</code>.',
        '<b>목표</b> — 텍스트 안에서 패턴이 처음 등장하는 인덱스(또는 모든 등장 위치, 또는 단순 존재 여부)를 반환하는 것.',
        '예: 텍스트가 <code>"ABABCABAB"</code>, 패턴이 <code>"ABC"</code>라면 → 패턴은 인덱스 2에서 발견된다.',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '가장 흔한 함정: <b>"찾으려는 문자열 = 텍스트"</b>라고 헷갈리는 것. ' +
        '반대다. <b>찾으려는 쪽이 패턴, 뒤지는 대상이 텍스트</b>다. ' +
        '"텍스트 = 대상, 패턴 = 찾는 것"으로 외워라.',
    },

    { type: 'h4', text: '검색에 정렬이 필요한가? (이진 검색과의 구분)' },
    {
      type: 'p',
      html:
        '문자열 검색은 <b>정렬이 전혀 필요 없다</b>. 텍스트를 정렬해 버리면 글자 순서가 ' +
        '망가져 패턴이 사라지므로, 오히려 정렬하면 안 된다. ' +
        '반면 <b>이진 검색(binary search)</b>은 <u>정렬된</u> 데이터에서 값을 반씩 좁혀 찾는 ' +
        '탐색법으로, 문자열 검색 알고리즘이 아니다. 마찬가지로 <b>병합 정렬</b>은 정렬 ' +
        '알고리즘일 뿐 문자열 검색과 무관하다.',
    },
    {
      type: 'table',
      headers: ['구분', '문자열 검색', '이진 검색'],
      rows: [
        ['대상', '정렬 안 된 문자열', '정렬된 배열/리스트'],
        ['찾는 것', '연속한 문자 패턴', '특정 값(키)'],
        ['정렬 필요?', '필요 없음', '반드시 필요'],
        ['대표 예', '브루트 포스 · KMP · 보이어-무어', '-'],
      ],
    },

    { type: 'h4', text: '공통 골격: 정렬 → 비교 → 이동' },
    {
      type: 'p',
      html:
        '세 알고리즘 모두 다음 루프를 돈다. ① 패턴을 텍스트의 어떤 시작 위치에 맞춘다(정렬). ' +
        '② 문자들을 비교한다. ③ 불일치하면 패턴을 오른쪽으로 옮긴다(shift). ' +
        '<b>차이는 오직 ③에서 "몇 칸" 옮기느냐</b>이다. 한 칸만 옮기면 브루트 포스, ' +
        '실패 함수만큼 옮기면 KMP, 불일치 규칙으로 크게 점프하면 보이어-무어다. ' +
        '이 한 문장이 이 단원 전체의 뼈대다.',
    },

    // ──────────────────────────────────────────────
    { type: 'h3', text: '2. 브루트 포스법 (Brute Force, 단순 비교)' },

    { type: 'h4', text: '정의 · 원리' },
    {
      type: 'p',
      html:
        '브루트 포스법은 <b>가장 단순한 문자열 검색 방법</b>이다. ' +
        '가능한 모든 시작 위치(텍스트 인덱스 0, 1, 2, …)에 패턴을 차례로 맞춰 보고, ' +
        '각 위치에서 <b>패턴의 앞 문자부터 한 글자씩</b> 비교한다. ' +
        '해시도, 트리도, 정렬도, 추가 표도 필요 없다. ' +
        '구현이 매우 쉽다는 것이 가장 큰 장점이다.',
    },

    { type: 'h4', text: '불일치가 났을 때: 패턴을 딱 한 칸 이동' },
    {
      type: 'p',
      html:
        '비교 도중 한 글자라도 불일치(mismatch)가 나면, 브루트 포스는 패턴을 ' +
        '<b>오른쪽으로 정확히 한 칸</b> 옮기고, <u>다시 패턴의 맨 앞부터</u> 비교를 시작한다. ' +
        '여러 칸 건너뛰지 않으며, 텍스트를 역순으로 보지도 않고, 탐색을 도중에 끝내지도 않는다. ' +
        '끝까지 일치하면 그 위치를 결과로 반환한다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '핵심 약점: 브루트 포스는 <b>이미 일치했던 비교 결과를 전혀 활용(재사용)하지 않는다</b>. ' +
        '앞쪽 몇 글자가 맞았다는 정보를 버리고 한 칸 옮겨 처음부터 다시 비교하므로, ' +
        '같은 문자를 여러 번 반복 비교하게 되어 비효율적이다. ' +
        '"재귀를 써서" 또는 "추가 배열 때문에" 느린 게 아니라, ' +
        '<b>비교 정보를 버리기 때문</b>에 느린 것이다.',
    },

    { type: 'h4', text: '예시 (한 칸 이동의 낭비)' },
    {
      type: 'p',
      html:
        '텍스트 <code>"AAAAAB"</code>, 패턴 <code>"AAAB"</code>를 생각하자. ' +
        '시작 위치 0에서 <code>AAA</code>까지 맞다가 4번째에서 불일치 → 패턴을 한 칸만 옮긴다. ' +
        '위치 1에서도 또 <code>AAA</code>를 다시 비교하고 또 마지막에서 불일치 → 또 한 칸. ' +
        '이렇게 거의 같은 비교를 위치마다 되풀이한다. 바로 이 반복이 최악의 비용을 만든다.',
    },

    { type: 'h4', text: '복잡도 · 주의' },
    {
      type: 'list',
      items: [
        '텍스트 길이 <code>n</code>, 패턴 길이 <code>m</code>일 때, 각 시작 위치(최대 n개)마다 최대 m번 비교 → <b>최악 시간복잡도 <code>O(nm)</code></b>.',
        '최선의 경우(첫 글자부터 계속 어긋남)는 위치당 1번만 비교해 <code>O(n)</code>에 가깝다.',
        '추가 메모리가 거의 필요 없다(전처리 표가 없다).',
        '주의: <code>O(1)</code>·<code>O(log n)</code>·<code>O(n)</code>이 아니라 최악은 <code>O(nm)</code>임을 반드시 구분하라.',
      ],
    },

    // ──────────────────────────────────────────────
    { type: 'h3', text: '3. KMP법 (Knuth–Morris–Pratt)' },

    { type: 'h4', text: '핵심 아이디어: 비교 결과의 재사용' },
    {
      type: 'p',
      html:
        'KMP의 한 줄 요약은 <b>"이미 비교해서 얻은 일치 정보를 재사용한다"</b>이다. ' +
        '브루트 포스가 불일치마다 정보를 버리고 한 칸 옮겨 처음부터 다시 비교하는 것과 정반대로, ' +
        'KMP는 <u>이미 맞았던 부분만큼은 다시 비교하지 않고</u> 패턴을 똑똑하게 건너뛴다. ' +
        '힙·해시·무작위 탐색·트리 균형 같은 것과는 아무 관련이 없다.',
    },

    { type: 'h4', text: '접두사(prefix) = 접미사(suffix) "경계(border)" 개념' },
    {
      type: 'p',
      html:
        'KMP의 재사용을 가능하게 하는 핵심은 패턴 자신의 구조다. 패턴의 어떤 부분 문자열에서 ' +
        '<b>앞쪽 일부(접두사)와 뒤쪽 일부(접미사)가 똑같이 겹치는 부분</b>을 미리 찾아 둔다. ' +
        '이렇게 접두사이면서 동시에 접미사인(자기 자신 전체는 제외) 가장 긴 부분을 ' +
        '<b>경계(border)</b>라고 부른다. ' +
        '불일치가 났을 때, 지금까지 맞았던 부분의 "접미사 = 더 짧은 접두사"인 만큼은 ' +
        '<u>다시 맞을 것이 보장</u>되므로, 그만큼 건너뛰어도 안전하다.',
    },
    {
      type: 'p',
      html:
        '예: 패턴 <code>"ABABAC"</code>에서 앞부분 <code>"ABAB"</code>는 접두사 <code>"AB"</code>가 ' +
        '접미사 <code>"AB"</code>와 같다(경계 길이 2). 그래서 <code>"ABAB"</code>까지 맞은 뒤 ' +
        '다음 글자에서 어긋나도, 앞의 <code>"AB"</code>는 이미 맞은 것으로 보고 패턴을 그 위치로 ' +
        '당겨 비교를 이어 간다.',
    },

    { type: 'h4', text: '실패 함수 / skip table 만드는 법' },
    {
      type: 'p',
      html:
        'KMP는 검색 전에 패턴만 보고 <b>skip table</b>(= 실패 함수, failure function)을 ' +
        '<u>한 번 전처리</u>한다. 이 표는 패턴의 각 위치 <code>i</code>까지 봤을 때 ' +
        '"접두사이자 접미사인 가장 긴 부분의 길이"를 담는다. 만드는 절차는 다음과 같다.',
    },
    {
      type: 'list',
      items: [
        '표의 0번 칸(패턴 첫 글자)은 항상 <code>0</code>으로 둔다(자기 자신만으로는 경계가 없음).',
        '패턴을 패턴 자신과 견주며, 접두사와 접미사가 일치하면 그 길이를 1 늘려 기록한다.',
        '일치가 깨지면, 그 직전까지의 경계 길이로 되돌아가(표를 다시 참조해) 더 짧은 경계를 찾는다.',
        '결과적으로 표의 <code>i</code>번 값 = "패턴[0..i]에서 접두사=접미사인 최대 길이".',
      ],
    },
    {
      type: 'table',
      headers: ['i (패턴 위치)', '0', '1', '2', '3', '4', '5'],
      rows: [
        ['패턴 문자', 'A', 'B', 'A', 'B', 'A', 'C'],
        ['skip table 값', '0', '0', '1', '2', '3', '0'],
      ],
    },
    {
      type: 'p',
      html:
        '위 표(<code>"ABABAC"</code>)에서 값이 점점 커지다(1→2→3) 마지막 <code>C</code>에서 ' +
        '0으로 떨어진 것을 보라. 이 값들이 곧 <b>불일치 시 패턴을 얼마나 앞으로 당겨 ' +
        '비교를 이어 갈지</b>를 알려 준다.',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        'skip table을 만드는 <b>목적</b>은 "메모리 절약"이나 "정렬", "트리 생성"이 아니라 ' +
        '오직 <b>중복 비교 제거</b>다. 같은 문자를 두 번 비교하지 않게 하려고 만드는 표라고 ' +
        '기억하면 시험 선지를 바로 거를 수 있다.',
    },

    { type: 'h4', text: '검색 동작: 불일치 시 skip table만큼 이동' },
    {
      type: 'p',
      html:
        '검색 중 불일치가 나면 KMP는 <b>처음부터 다시 비교하지 않는다</b>. ' +
        '대신 skip table을 참고해 패턴을 "이미 맞은 경계 길이" 위치로 당기고, ' +
        '<u>텍스트의 그 자리</u>부터 비교를 이어 간다. ' +
        '즉 불일치가 났다고 텍스트를 거슬러 올라가 다시 보지 않는다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        'KMP의 가장 큰 특징이자 자주 출제되는 포인트: <b>텍스트 포인터는 절대 뒤로 후퇴하지 않는다</b>. ' +
        '움직이는 것은 패턴 쪽 인덱스뿐이다. 그래서 텍스트는 끝까지 단 한 번만 훑게 되고, ' +
        '"불일치하면 항상 처음부터 다시 비교한다"는 설명은 <b>틀린</b> 설명이다(그건 브루트 포스).',
    },

    { type: 'h4', text: '복잡도' },
    {
      type: 'list',
      items: [
        'skip table 전처리: 패턴만 보므로 <code>O(m)</code>.',
        '검색: 텍스트 포인터가 후퇴하지 않아 텍스트를 한 번만 지나가므로 <code>O(n)</code>.',
        '<b>전체 시간복잡도 <code>O(n + m)</code> (선형)</b>. <code>O(1)</code>·<code>O(log n)</code>·<code>O(n²)</code>가 아니다.',
        '추가 메모리: skip table용 <code>O(m)</code>.',
      ],
    },

    { type: 'viz', component: 'BFvsKMPViz' },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '위 시각화는 같은 입력에서 <b>브루트 포스 vs KMP의 비교 횟수</b>를 나란히 대조한다. ' +
        '브루트 포스가 같은 자리를 반복 비교하는 동안 KMP가 어떻게 건너뛰어 비교 수를 ' +
        '줄이는지 눈으로 확인해 보라.',
    },

    // ──────────────────────────────────────────────
    { type: 'h3', text: '4. 보이어-무어법 (Boyer–Moore)' },

    { type: 'h4', text: '독특한 점: 패턴의 끝 문자부터 비교' },
    {
      type: 'p',
      html:
        '보이어-무어법은 다른 두 방법과 달리 패턴을 <b>뒤(끝 문자)에서부터 앞으로</b> ' +
        '비교한다. 패턴을 텍스트의 한 위치에 맞춘 뒤, 패턴의 <u>마지막 글자</u>와 ' +
        '그에 대응하는 텍스트 글자를 먼저 비교하고, 맞으면 한 칸씩 앞으로 비교를 이어 간다. ' +
        '"패턴 앞 문자부터" 비교한다는 설명은 틀린 설명이다.',
    },

    { type: 'h4', text: '불일치 문자 규칙 (bad character) — 큰 점프의 비밀' },
    {
      type: 'p',
      html:
        '뒤에서부터 비교하다 불일치가 나면, 그때 텍스트 쪽 글자(불일치 문자)를 본다. ' +
        '<b>그 문자가 패턴 안에 아예 없으면</b>, 그 위치에 패턴의 어떤 글자를 갖다 대도 맞을 ' +
        '리 없으므로 <u>패턴 길이 m만큼 통째로 건너뛸 수 있다</u>. ' +
        '그 문자가 패턴 안에 있으면, 패턴에서 그 문자가 나오는 자리에 맞춰지도록 점프한다. ' +
        '이렇게 불일치 정보에 따라 <b>여러 칸을 한 번에 건너뛰는 것</b>이 보이어-무어의 ' +
        '빠름의 원천이다.',
    },
    {
      type: 'p',
      html:
        '예: 텍스트 <code>"...XYZ..."</code> 구간에서 패턴의 끝 글자와 텍스트의 <code>Z</code>가 ' +
        '불일치인데 패턴에 <code>Z</code>가 전혀 없다면, 중간 글자들을 <b>아예 보지도 않고</b> ' +
        '패턴 전체 길이만큼 건너뛴다. 평균적으로 텍스트의 많은 글자를 건너뛰며 지나가므로 ' +
        '<b>실제 수행 속도가 가장 빠른 편</b>이다.',
    },

    { type: 'viz', component: 'BoyerMooreViz' },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '위 시각화에서 보이어-무어가 <b>패턴 뒤에서부터 비교</b>하다가 불일치 문자를 만나 ' +
        '<b>큰 폭으로 점프</b>하는 모습을 단계별로 확인할 수 있다. 건너뛰는 글자 수가 ' +
        '브루트 포스의 "한 칸"과 얼마나 다른지에 주목하라.',
    },

    { type: 'h4', text: '복잡도 · 주의' },
    {
      type: 'list',
      items: [
        '평균적으로는 많은 글자를 건너뛰어 매우 빠르며, 좋은 경우 <code>O(n/m)</code>에 가깝게 동작하기도 한다.',
        '하지만 <b>최악의 경우</b>(점프가 잘 안 되는 입력)에는 여전히 <code>O(nm)</code>가 될 수 있다.',
        '"실제(평균) 속도가 빠르다"는 것이지 "항상 이론적 최악이 가장 좋다"는 뜻은 아니다.',
        '구현은 KMP보다 단순하지 않다. "구현이 가장 간단함"이나 "추가 메모리를 전혀 안 씀"은 보이어-무어의 핵심 장점이 아니다(불일치 문자 규칙용 표가 필요).',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '혼동 주의: <b>가장 빠른(평균)</b> = 보이어-무어, <b>가장 단순한</b> = 브루트 포스. ' +
        '두 장점을 한 알고리즘에 몰아 주는 선지에 속지 말 것. 보이어-무어의 장점은 ' +
        '"끝에서 비교 + 큰 점프로 평균이 빠름"이다.',
    },

    // ──────────────────────────────────────────────
    { type: 'h3', text: '5. 세 방법 총정리 비교' },

    { type: 'h4', text: '한눈 비교표' },
    {
      type: 'table',
      headers: ['알고리즘', '비교 시작 위치', '불일치 시 이동', '비교 정보 재사용', '최악 시간복잡도', '핵심 특징'],
      rows: [
        ['브루트 포스', '패턴 앞에서부터', '한 칸씩', '안 함(버림)', 'O(nm)', '가장 단순, 구현 쉬움'],
        ['KMP', '패턴 앞에서부터', 'skip table 길이만큼', '함(접두사=접미사)', 'O(n+m)', '텍스트 포인터 비후퇴'],
        ['보이어-무어', '패턴 <b>끝</b>에서부터', '불일치 문자 규칙으로 큰 점프', '부분적(끝 비교)', 'O(nm)~평균 O(n/m)', '평균 실제 속도 가장 빠름'],
      ],
    },

    { type: 'h4', text: '시험에서 꼭 구분할 점' },
    {
      type: 'list',
      items: [
        '<b>가장 단순/구현 쉬움</b> → 브루트 포스. (KMP·보이어-무어는 전처리 표가 필요)',
        '<b>비교 결과 재사용 / skip table(실패 함수) / O(n+m) / 텍스트 포인터 비후퇴</b> → KMP.',
        '<b>패턴 끝에서부터 비교 / 큰 폭 점프 / 평균 실제 속도 빠름</b> → 보이어-무어.',
        '<b>O(nm)이 최악인 것</b> → 브루트 포스(항상)와 보이어-무어(최악). KMP는 O(n+m).',
        '병합 정렬·이진 검색은 문자열 검색 알고리즘이 <u>아니다</u>.',
        '문자열 검색에는 정렬이 필요 없으며, 패턴의 "존재 여부"만 확인하는 용도로도 쓸 수 있다.',
      ],
    },

    { type: 'viz', component: 'StringSearchVisualizer' },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '메인 시각화에서 텍스트와 패턴을 직접 입력하고 <b>브루트 포스 / KMP</b>를 단계 실행해 ' +
        '보세요. 포인터 이동과 일치(초록)·불일치(빨강), KMP의 skip table 동작, ' +
        '그리고 텍스트 포인터가 KMP에서는 되돌아가지 않는다는 점을 직접 확인할 수 있습니다.',
    },
  ],
}
