// Ch06 정렬 알고리즘 — 세분화 개념 정리 (WF2)
// 전공(CS) 수준. 평가 3축 · 비교정렬 하한 · 단순정렬 · 쉘 · 분할정복 · 힙 · 도수 · 안정성 요인 · 선택 기준

export const concept = {
  summary:
    '정렬(sorting)은 데이터를 정해진 키 기준에 따라 순서대로 재배치하는 연산이다. 모든 정렬은 ' +
    '시간복잡도(최선·평균·최악), 안정성(stable), 제자리(in-place) 라는 세 축으로 평가·선택한다. ' +
    '원소끼리 대소를 비교하는 비교 기반 정렬은 평균적으로 Ω(n log n)이라는 이론적 하한을 벗어날 수 없고, ' +
    '이를 깨려면 값의 분포를 직접 이용하는 비비교 정렬(도수 정렬)을 써야 한다. 이 장에서는 단순 정렬 ' +
    '3종(버블·선택·삽입), 쉘 정렬, 분할정복 2종(퀵·병합), 힙 정렬, 도수 정렬의 동작 원리와 ' +
    '비교·교환·이동 비용, 안정성/제자리 여부를 정확히 구분한다.',
  blocks: [
    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '1. 정렬을 평가하는 3가지 축' },
    {
      type: 'p',
      html:
        '같은 결과(오름차순/내림차순)를 내는 정렬이 8가지나 존재하는 이유는, 상황마다 <b>유리한 알고리즘이 다르기</b> ' +
        '때문이다. 어떤 정렬을 쓸지는 항상 다음 세 축으로 따진다. 시험은 이 세 축을 알고리즘별로 정확히 외우고 ' +
        '있는지를 묻는다.',
    },

    { type: 'h4', text: '1-1. 시간복잡도 (최선 · 평균 · 최악)' },
    {
      type: 'p',
      html:
        '입력 크기 <code>n</code>이 커질 때 연산량(주로 <b>비교</b>와 <b>이동/교환</b> 횟수)이 어떻게 증가하는지를 ' +
        '점근 표기 O로 나타낸다. 중요한 것은 같은 알고리즘이라도 입력 상태에 따라 성능이 달라질 수 있어 ' +
        '<b>최선(best) · 평균(average) · 최악(worst)</b>을 구분해야 한다는 점이다.',
    },
    {
      type: 'list',
      items: [
        '<b>최선</b> — 입력이 알고리즘에 가장 유리할 때. 예: 삽입 정렬은 이미 정렬된 입력에서 <code>O(n)</code>.',
        '<b>최악</b> — 가장 불리할 때. 예: 퀵 정렬은 분할이 한쪽으로 치우치면 <code>O(n²)</code>.',
        '<b>평균</b> — 무작위 입력에 대한 기댓값. 실무 성능의 기준이 되는 값.',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>흔한 함정:</b> "퀵 정렬은 빠르니까 항상 O(n log n)"은 <b>틀린 말</b>이다. 평균은 O(n log n)이지만 ' +
        '최악은 O(n²)이다. 반대로 "병합·힙 정렬은 입력과 무관하게 항상 O(n log n)"은 맞다. 문제에서 ' +
        '<b>"항상"</b>이라는 단어가 나오면 최악까지 O(n log n)인 정렬(병합·힙)을 떠올려라.',
    },

    { type: 'h4', text: '1-2. 안정성 (stable)' },
    {
      type: 'p',
      html:
        '<b>키가 같은 원소들의 원래 상대 순서가 정렬 후에도 보존</b>되면 그 정렬은 <b>안정(stable)</b>하다. ' +
        '예를 들어 입력이 <code>(2,A) (2,C)</code> 순서였다면, 키 2끼리는 정렬 후에도 A가 C보다 앞에 있어야 안정 ' +
        '정렬이다. 단일 숫자만 정렬할 때는 안정성이 보이지 않지만, <b>(키, 부가데이터)</b> 쌍을 정렬하거나 ' +
        '<b>여러 기준으로 다단계 정렬</b>(예: 먼저 이름순, 그다음 점수순)할 때 결정적으로 중요하다.',
    },
    {
      type: 'table',
      headers: ['구분', '안정 정렬', '불안정 정렬'],
      rows: [
        ['해당 알고리즘', '버블, 삽입, 병합, 도수', '선택, 쉘, 퀵, 힙'],
        ['특징', '같은 키의 입력 순서를 보존', '먼 거리 교환이 같은 키의 순서를 뒤섞을 수 있음'],
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>불안정의 원인은 "멀리 떨어진 원소를 건너뛰며 교환"하기 때문</b>이다. 선택 정렬은 최솟값을 먼 위치에서 ' +
        '끌어오며 교환하다가 같은 키의 순서를 깨고, 퀵·힙·쉘도 큰 보폭의 교환으로 같은 키를 뒤집을 수 있다. ' +
        '반면 버블·삽입은 인접 원소만, 그것도 <b>"더 클 때만"</b> 교환하므로 같은 키를 넘지 않아 안정적이다.',
    },

    { type: 'h4', text: '1-3. 제자리 (in-place)' },
    {
      type: 'p',
      html:
        '입력 배열 외에 <b>거의 추가 메모리를 쓰지 않으면</b>(보통 <code>O(1)</code>, 재귀 스택까지 포함해 ' +
        '<code>O(log n)</code> 정도면 통상 제자리로 본다) <b>제자리(in-place)</b> 정렬이다. 반대로 입력 크기에 ' +
        '비례하는 보조 배열 <code>O(n)</code>이 필요하면 제자리가 아니다.',
    },
    {
      type: 'list',
      items: [
        '<b>제자리 O:</b> 버블, 선택, 삽입, 쉘, 퀵, 힙 — 배열 안에서 교환만 한다.',
        '<b>제자리 X:</b> 병합 정렬(병합용 보조 배열 O(n)), 도수 정렬(count 배열 O(k) + 출력 배열 O(n)).',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>혼동 주의:</b> 퀵 정렬은 분할정복이라 "재귀=메모리 많이 쓴다"고 오해하기 쉽지만, 표준 구현은 ' +
        '<b>배열 안에서 직접 분할(in-place partition)</b>하므로 제자리 정렬로 분류한다(재귀 스택 O(log n)). ' +
        '병합 정렬이 제자리가 아닌 진짜 이유는 재귀가 아니라 <b>병합 단계의 보조 배열</b> 때문이다.',
    },

    { type: 'h3', text: '2. 비교 정렬의 하한 Ω(n log n)' },
    {
      type: 'p',
      html:
        '원소끼리 <b>대소 비교</b>만으로 순서를 정하는 모든 정렬을 <b>비교 기반 정렬</b>이라 한다. 비교 한 번은 ' +
        '"예/아니오" 두 갈래로 갈리므로, 정렬 과정을 <b>결정 트리(decision tree)</b>로 그릴 수 있다. n개 원소의 ' +
        '가능한 순열은 <code>n!</code>가지이고, 트리의 잎(leaf)은 이 n!개의 결과를 모두 담아야 한다.',
    },
    {
      type: 'p',
      html:
        '잎이 <code>n!</code>개인 이진 트리의 높이는 최소 <code>log₂(n!)</code>이고, 스털링 근사에 의해 ' +
        '<code>log₂(n!) = Θ(n log n)</code>이다. 트리의 높이는 곧 <b>최악의 비교 횟수</b>이므로, 어떤 비교 정렬도 ' +
        '최악의 경우 <code>Ω(n log n)</code>번의 비교가 필요하다. 즉 <b>비교만으로는 평균적으로 이보다 빠를 수 없다.</b>',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '이 하한은 <b>비교 정렬</b>에만 적용된다. 도수 정렬처럼 비교 대신 <b>값 자체를 인덱스로 사용</b>하는 ' +
        '비비교 정렬은 이 하한의 적용 대상이 아니므로 <code>O(n+k)</code>로 n log n을 깰 수 있다.',
    },

    { type: 'h3', text: '3. 8가지 대표 정렬 한눈에 보기' },
    {
      type: 'table',
      headers: ['알고리즘', '최선', '평균', '최악', '안정성', '제자리', '핵심 아이디어'],
      rows: [
        ['버블 정렬', 'O(n)*', 'O(n²)', 'O(n²)', '안정', 'O', '인접한 두 원소를 비교·교환해 큰 값을 뒤로'],
        ['선택 정렬', 'O(n²)', 'O(n²)', 'O(n²)', '불안정', 'O', '남은 구간의 최솟값을 골라 앞에 배치'],
        ['삽입 정렬', 'O(n)', 'O(n²)', 'O(n²)', '안정', 'O', '앞쪽 정렬 구간에 새 원소를 끼워 넣기'],
        ['쉘 정렬', 'O(n log n)~', 'O(n^1.3~1.5)', 'O(n²)', '불안정', 'O', 'gap 간격 삽입 정렬을 점점 좁혀가며 반복'],
        ['퀵 정렬', 'O(n log n)', 'O(n log n)', 'O(n²)', '불안정', 'O', 'pivot 기준 분할 후 양쪽 재귀(분할정복)'],
        ['병합 정렬', 'O(n log n)', 'O(n log n)', 'O(n log n)', '안정', 'X', '반으로 나눠 정렬 후 병합(보조 배열)'],
        ['힙 정렬', 'O(n log n)', 'O(n log n)', 'O(n log n)', '불안정', 'O', '최대 힙에서 루트(최댓값)를 반복해 빼냄'],
        ['도수 정렬', 'O(n+k)', 'O(n+k)', 'O(n+k)', '안정', 'X', '값별 개수를 세고 누적합으로 위치 결정(비비교)'],
      ],
    },
    {
      type: 'p',
      html:
        '* 버블 정렬의 <code>O(n)</code> 최선은 <b>조기 종료(swapped 플래그) 최적화를 넣었을 때</b>, 이미 정렬된 입력에 ' +
        '한해 성립한다. 최적화가 없으면 입력과 무관하게 항상 <code>O(n²)</code>이다. 아래 인터랙티브 표에서 8종을 ' +
        '직접 비교해 보라.',
    },
    { type: 'viz', component: 'ComplexityChart' },
    {
      type: 'p',
      html:
        '복잡도 표가 추상적으로 느껴진다면, 아래 <b>레이스 시뮬레이션</b>에서 여러 정렬을 같은 배열로 동시에 돌려 ' +
        '어느 알고리즘이 더 적은 단계로 먼저 끝나는지 직접 눈으로 확인해 보라.',
    },
    { type: 'viz', component: 'SortRace' },

    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '4. 단순 정렬 — 버블 · 선택 · 삽입 (O(n²))' },
    {
      type: 'p',
      html:
        '세 알고리즘 모두 평균·최악 <code>O(n²)</code>이지만, <b>비교 횟수 · 교환(이동) 횟수 · 안정성 · 입력 민감도</b>가 ' +
        '서로 다르다. 시험은 이 미세한 차이를 집요하게 묻는다.',
    },

    { type: 'h4', text: '4-1. 버블 정렬 (Bubble Sort)' },
    {
      type: 'p',
      html:
        '<b>정의·원리:</b> 배열을 왼쪽부터 훑으며 <b>인접한 두 원소</b> <code>arr[j]</code>, <code>arr[j+1]</code>를 ' +
        '비교해 앞이 더 크면 교환한다. 한 번의 바깥 회전(pass)이 끝나면 가장 큰 값이 거품처럼 맨 뒤로 떠오른다. ' +
        '<code>i</code>회전이 끝나면 뒤쪽 <code>i</code>개는 이미 제자리이므로 안쪽 루프 범위를 ' +
        '<code>len-1-i</code>로 줄인다.',
    },
    {
      type: 'p',
      html:
        '<b>비용:</b> 비교 횟수는 입력과 무관하게 <code>(n-1)+(n-2)+…+1 = n(n-1)/2</code>로 일정하다(최적화 없을 때). ' +
        '예를 들어 <code>n=4</code>면 <code>3+2+1 = 6</code>번 비교한다. 교환 횟수는 입력에 따라 0회(정렬 완료)부터 ' +
        '<code>n(n-1)/2</code>회(역순)까지 변한다.',
    },
    {
      type: 'p',
      html:
        '<b>조기 종료 최적화:</b> 한 회전 동안 교환이 한 번도 없었다면(<code>swapped == False</code>) 이미 정렬이 ' +
        '끝난 것이므로 즉시 <code>break</code>한다. 이 최적화가 있으면 <b>이미 정렬된 입력</b>은 첫 회전 ' +
        '<code>n-1</code>번 비교만 하고 멈춰 최선 <code>O(n)</code>이 된다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>추적 함정:</b> 바깥 루프 횟수가 제한된 코드(예: <code>for i in range(2)</code>)는 정렬을 <b>끝까지 ' +
        '하지 않는다.</b> "최종 정렬 결과"가 아니라 "2회전까지만 진행한 중간 상태"를 묻는 것이니, 회전을 회차별로 ' +
        '손으로 추적하라. 버블은 인접 비교만 하므로 <b>같은 키를 추월하지 않아 안정 정렬</b>이다.',
    },

    { type: 'h4', text: '4-2. 선택 정렬 (Selection Sort)' },
    {
      type: 'p',
      html:
        '<b>정의·원리:</b> 매 회차 <b>남은 구간 전체를 훑어 최솟값의 위치(min_idx)를 찾고</b>, 그 값을 현재 회차의 맨 ' +
        '앞(<code>i</code>)과 한 번 교환한다. 핵심 교환문은 <code>arr[i], arr[min_idx] = arr[min_idx], arr[i]</code> ' +
        '이다(두 빈칸 모두 <code>min_idx</code>).',
    },
    {
      type: 'p',
      html:
        '<b>비용:</b> 최솟값을 찾으려면 매번 구간 전체를 비교해야 하므로 <b>비교 횟수가 입력 상태와 무관하게 항상 ' +
        '</b><code>n(n-1)/2</code>로 고정된다. 예: <code>n=5</code>면 <code>4+3+2+1 = 10</code>번. 반면 ' +
        '<b>교환은 회차당 정확히 1번</b>(최대 <code>n-1</code>회)뿐이라, 데이터 이동 비용이 매우 작은 것이 장점이다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>대표 오개념:</b> "선택 정렬은 입력 상태에 따라 빨라진다"는 <b>틀렸다.</b> 거의 정렬돼 있어도 ' +
        '최솟값을 확인하려고 매번 구간 전체를 비교하므로 비교 횟수가 줄지 않는다. 또한 먼 위치의 값을 끌어와 ' +
        '교환하므로 <b>같은 키의 순서가 깨질 수 있어 불안정 정렬</b>이다(이 점이 삽입 정렬과의 핵심 차이).',
    },

    { type: 'h4', text: '4-3. 삽입 정렬 (Insertion Sort)' },
    {
      type: 'p',
      html:
        '<b>정의·원리:</b> 앞쪽 <code>[0..i-1]</code>이 <b>이미 정렬되어 있다고 보고</b>, 새 원소 ' +
        '<code>key = arr[i]</code>를 정렬된 구간의 알맞은 자리에 끼워 넣는다. <code>key</code>보다 큰 값들을 ' +
        '<b>한 칸씩 오른쪽으로 밀고</b>(이동), 빈자리에 <code>key</code>를 놓는다. while 종료 후 <code>j</code>는 ' +
        'key 이하 값의 위치이므로 <b>key는 <code>arr[j+1]</code>에 들어간다</b>(빈칸 추론 단골).',
    },
    {
      type: 'p',
      html:
        '<b>비용:</b> 비교·이동 횟수가 입력 상태에 민감하다. <b>이미 정렬된 입력</b>이면 각 key가 바로 제자리라 ' +
        '비교 1번·이동 0번으로 끝나 최선 <code>O(n)</code>. <b>역순 입력</b>이면 매 key가 맨 앞까지 밀려 ' +
        '<code>1+2+…+(n-1) = n(n-1)/2</code>번 이동해 최악 <code>O(n²)</code>. 그래서 <b>거의 정렬된 데이터에서 ' +
        '가장 효율적</b>이다.',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '삽입 정렬은 <b>안정 정렬</b>이다. 비교 조건이 <code>arr[j] &gt; key</code>(<b>같으면 멈춤</b>)이라, 같은 키를 ' +
        '추월하지 않고 입력 순서를 보존하기 때문이다. 선택 정렬과 비교하면 — <b>선택</b>은 비교 일정·이동 적음·불안정, ' +
        '<b>삽입</b>은 비교/이동이 입력에 민감·안정 — 으로 대비된다.',
    },
    {
      type: 'table',
      headers: ['항목', '버블', '선택', '삽입'],
      rows: [
        ['비교 횟수(최악)', 'n(n-1)/2', 'n(n-1)/2 (항상)', 'n(n-1)/2'],
        ['최선 시간복잡도', 'O(n) (조기 종료 시)', 'O(n²)', 'O(n)'],
        ['교환/이동 특징', '교환 많음(역순 시)', '교환 회차당 1번', '이동 많음(역순 시)'],
        ['입력 상태 민감도', '민감(조기 종료)', '둔감(항상 동일)', '매우 민감'],
        ['안정성', '안정', '불안정', '안정'],
        ['유리한 상황', '교육용·소규모', '교환 비용이 클 때', '거의 정렬된 데이터'],
      ],
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '아래 <b>SortVisualizer</b>에서 배열을 직접 입력하고 알고리즘을 골라 <b>단계 실행</b>해 보라. ' +
        '비교(노랑) · 교환(빨강) · 정렬 완료(초록) 색으로 위 세 정렬의 비교/이동 차이를 눈으로 확인할 수 있다.',
    },
    { type: 'viz', component: 'SortVisualizer' },

    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '5. 쉘 정렬 (Shell Sort)' },
    {
      type: 'p',
      html:
        '<b>동기:</b> 삽입 정렬은 인접 한 칸씩만 이동하므로, 멀리 떨어진 작은 값이 제자리로 가려면 수많은 이동이 ' +
        '필요하다. <b>쉘 정렬</b>은 이 한계를 <code>gap</code> 간격으로 떨어진 원소들끼리 먼저 정렬해 해결한다.',
    },
    {
      type: 'p',
      html:
        '<b>원리:</b> 큰 <code>gap</code>으로 시작해 <code>gap</code>만큼 떨어진 원소들로 이루어진 부분 수열들을 각각 ' +
        '삽입 정렬한다(예: gap=3이면 인덱스 (0,3), (1,4), (2,5)끼리 비교). 그다음 <code>gap</code>을 점점 줄여 ' +
        '(예: 3 → 1) 반복하고, <b>마지막 gap=1은 일반 삽입 정렬</b>로 마무리한다. 큰 보폭으로 미리 대강 정렬해 ' +
        '두므로 마지막 단계의 이동량이 크게 줄어든다.',
    },
    {
      type: 'p',
      html:
        '<b>복잡도·주의:</b> 성능은 gap 수열에 좌우된다. 평균은 대략 <code>O(n^1.3~1.5)</code>로 단순 삽입보다 빠르고, ' +
        '최악은 <code>O(n²)</code>이다. gap이 1보다 큰 단계에서 <b>먼 거리 교환</b>이 일어나 같은 키의 순서를 ' +
        '바꿀 수 있어 <b>불안정 정렬</b>이다. 추가 메모리는 거의 쓰지 않아 <b>제자리</b>다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>추적 함정:</b> "gap=3으로 한 번만 돈 결과"는 <b>완전한 정렬이 아니다.</b> gap 한 패스만 처리한 코드는 ' +
        '부분 수열 안에서만 정렬되므로 중간 상태가 나온다. <code>while j &gt;= gap and arr[j-gap] &gt; temp</code> ' +
        '조건을 gap 단위로 손으로 따라가며 추적하라.',
    },

    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '6. 분할정복 — 퀵 · 병합' },
    {
      type: 'p',
      html:
        '<b>분할정복(divide and conquer):</b> 문제를 작은 부분 문제로 <b>나누고(divide)</b>, 각각 풀어 ' +
        '<b>정복(conquer)</b>한 뒤 <b>합친다(combine)</b>. 퀵과 병합은 둘 다 분할정복이지만, "어디에 힘을 쓰는가"가 ' +
        '정반대다 — 퀵은 <b>나누는 단계(partition)</b>에서, 병합은 <b>합치는 단계(merge)</b>에서 일을 한다.',
    },

    { type: 'h4', text: '6-1. 퀵 정렬 (Quick Sort)' },
    {
      type: 'p',
      html:
        '<b>원리:</b> 기준값 <code>pivot</code>을 하나 골라, pivot보다 작은 값은 왼쪽 / 크거나 같은 값은 오른쪽으로 ' +
        '<b>분할(partition)</b>한다. 그러면 pivot은 정렬 후 제자리를 확정받는다. 이후 왼쪽·오른쪽 부분 배열을 ' +
        '<b>재귀</b>로 같은 방식으로 정렬한다. 개념적 형태는 ' +
        '<code>quick_sort(left) + [pivot] + quick_sort(right)</code>이다(가운데 빈칸 단골 답은 <code>pivot</code>).',
    },
    {
      type: 'p',
      html:
        '<b>제자리 분할:</b> 실무 구현은 보조 리스트 없이 <b>배열 안에서 두 포인터(left/right)를 좁혀가며 직접 ' +
        '교환</b>해 분할한다. 마지막에 pivot을 경계 위치와 교환하면 pivot이 제자리에 놓인다. 그래서 퀵은 ' +
        '<b>제자리 정렬</b>로 분류된다.',
    },
    {
      type: 'p',
      html:
        '<b>복잡도:</b> 분할이 매번 절반에 가깝게 균형 잡히면 깊이 <code>O(log n)</code> × 각 레벨 <code>O(n)</code> = ' +
        '평균 <code>O(n log n)</code>. 그러나 <b>분할이 한쪽으로 치우치면</b> 깊이가 <code>O(n)</code>이 되어 ' +
        '최악 <code>O(n²)</code>.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>최악의 트리거:</b> <b>이미 정렬된(또는 역순) 입력 + 나쁜 pivot 선택</b>(예: 항상 첫/끝 원소를 pivot으로). ' +
        '이때 한쪽 분할이 비고 다른 쪽만 n-1개가 되어 분할이 n번 반복돼 <code>O(n²)</code>이 된다. ' +
        'random pivot이나 median-of-three로 pivot을 고르면 이 함정을 피한다. 또한 먼 거리 교환 탓에 ' +
        '<b>불안정 정렬</b>이다.',
    },

    { type: 'h4', text: '6-2. 병합 정렬 (Merge Sort)' },
    {
      type: 'p',
      html:
        '<b>원리:</b> 배열을 <b>무조건 절반으로</b> 나눠 더 이상 못 나눌 때까지(길이 1) 재귀로 쪼갠 뒤, 두 정렬된 ' +
        '부분을 <b>병합(merge)</b>한다. merge는 두 부분의 앞에서부터 작은 값을 골라 결과 배열에 차례로 넣고, 한쪽이 ' +
        '소진되면 나머지를 통째로 붙인다. 예: <code>[1,4,7]</code>과 <code>[2,3,6]</code> → ' +
        '<code>[1,2,3,4,6,7]</code>.',
    },
    {
      type: 'p',
      html:
        '<b>복잡도:</b> 분할 깊이는 입력과 무관하게 항상 <code>log n</code>이고 각 레벨 병합이 <code>O(n)</code>이므로, ' +
        '<b>최선·평균·최악이 모두 </b><code>O(n log n)</code>으로 일정하다. 입력 상태에 흔들리지 않는 안정적 성능이 ' +
        '최대 장점이다.',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '<b>안정성:</b> merge에서 비교를 <code>left[i] &lt;= right[j]</code>처럼 <b>"같으면 왼쪽(앞 블록)을 먼저"</b> ' +
        '넣으면, 같은 키에서 입력 순서가 보존돼 <b>안정 정렬</b>이 된다. 만약 <code>&lt;</code>로만 비교하면 같은 키일 때 ' +
        'right가 먼저 들어가 안정성이 깨질 수 있다(안정성은 <b>비교/처리 방식</b>에 달려 있다는 대표 예).',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>유일한 약점:</b> 병합 결과를 담을 <b>보조 배열 O(n)</b>이 필요해 <b>제자리 정렬이 아니다.</b> 메모리가 ' +
        '빠듯한 환경에서 퀵·힙 대신 병합을 고를 때 반드시 따져야 할 점이다.',
    },
    {
      type: 'table',
      headers: ['항목', '퀵 정렬', '병합 정렬'],
      rows: [
        ['힘을 쓰는 단계', '분할(partition)', '병합(merge)'],
        ['평균', 'O(n log n)', 'O(n log n)'],
        ['최악', 'O(n²) (편향 분할)', 'O(n log n) (항상)'],
        ['안정성', '불안정', '안정'],
        ['제자리', 'O (in-place partition)', 'X (보조 배열 O(n))'],
        ['약점', '나쁜 pivot 시 O(n²)', '추가 메모리 필요'],
      ],
    },

    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '7. 힙 정렬 (Heap Sort)' },
    {
      type: 'p',
      html:
        '<b>완전이진트리와 배열:</b> 힙 정렬은 배열을 <b>완전이진트리(complete binary tree)</b>로 해석한다. ' +
        '0-기반 인덱스에서 부모가 <code>i</code>이면 <b>왼쪽 자식 <code>2i+1</code>, 오른쪽 자식 ' +
        '<code>2i+2</code></b>, 자식의 부모는 <code>(i-1)/2</code>다. 트리가 빈틈없이 채워지므로 추가 포인터 없이 ' +
        '배열만으로 표현할 수 있다(제자리).',
    },
    {
      type: 'p',
      html:
        '<b>최대 힙(max heap):</b> 모든 부모가 자식보다 크거나 같은 상태. 루트(<code>arr[0]</code>)에는 항상 ' +
        '<b>전체 최댓값</b>이 온다. 정렬 아이디어는 — 루트(최댓값)를 떼어 맨 뒤로 보내고, 힙 크기를 줄인 뒤 다시 ' +
        '힙 성질을 회복 — 을 반복하는 것이다.',
    },
    { type: 'h4', text: '7-1. down-heap (sift-down)과 heapify' },
    {
      type: 'p',
      html:
        '<b>down-heap:</b> 어떤 부모가 힙 성질을 어겼을 때, <b>두 자식 중 더 큰 자식</b>과 비교해 부모가 작으면 ' +
        '교환하고, 교환된 위치에서 같은 과정을 자식 방향으로 반복한다. 한 번의 down-heap 비용은 트리 높이만큼 ' +
        '<code>O(log n)</code>이다. 코드에서 <code>child+1 &lt; n and arr[child] &lt; arr[child+1]</code>로 ' +
        '<b>더 큰 자식</b>을 먼저 고르는 부분이 핵심이다.',
    },
    {
      type: 'p',
      html:
        '<b>build heap(heapify):</b> 무작위 배열을 최대 힙으로 만드는 단계. 잎 노드는 이미 힙이므로, ' +
        '<b>마지막 내부 노드 <code>n/2 - 1</code>부터 루트까지 거꾸로(bottom-up)</b> down-heap을 적용한다.',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '<b>build heap의 복잡도는 <code>O(n)</code>이다</b>(O(n log n) 아님!). 노드가 많은 아래쪽은 ' +
        '이동 거리가 짧고, 이동 거리가 긴 위쪽은 노드가 적어, 전체 비용 합이 <code>Σ (높이별 노드 수 × 높이)</code> ' +
        '= <code>O(n)</code>으로 수렴하기 때문이다. 다만 <b>힙 정렬 전체</b>는 build heap O(n) + (루트 추출 × n회 ' +
        '× down-heap O(log n)) = <code>O(n log n)</code>이다.',
    },
    {
      type: 'p',
      html:
        '<b>안정성·제자리:</b> 루트를 맨 뒤와 교환하는 과정에서 <b>먼 거리 교환</b>이 일어나 같은 키의 순서가 ' +
        '깨질 수 있어 <b>불안정 정렬</b>이다. 배열 안에서만 교환하므로 <b>제자리 정렬</b>이며, 보조 메모리는 ' +
        '<code>O(1)</code>이다.',
    },

    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '8. 도수 정렬 (Counting Sort) — 비비교 정렬' },
    {
      type: 'p',
      html:
        '<b>원리:</b> 값을 <b>비교하지 않고</b>, 값 자체를 인덱스로 사용한다. ① 값의 범위 <code>k</code>만큼 ' +
        '<code>count</code> 배열을 만들어 <b>각 값이 몇 번 나오는지 센다(도수 배열)</b>. ② count를 <b>누적합</b>으로 ' +
        '바꿔 "각 값이 출력 배열에서 끝나는 위치"를 구한다. ③ 입력을 <b>뒤에서부터</b> 읽어 누적 도수가 가리키는 ' +
        '자리에 배치한다.',
    },
    {
      type: 'list',
      items: [
        '<b>도수 배열 예:</b> <code>[2,0,1,2,1,0,2]</code> → 0이 2개, 1이 2개, 2가 3개 → <code>count = [2,2,3]</code>.',
        '<b>누적합 예:</b> <code>[2,2,3]</code> → <code>count[1]=2+2=4</code>, <code>count[2]=3+4=7</code> → <code>[2,4,7]</code>.',
        '누적합 값은 "그 값까지의 원소 개수" = 출력 배열에서의 끝 경계 위치를 뜻한다.',
      ],
    },
    {
      type: 'p',
      html:
        '<b>복잡도:</b> 값을 세고(n) 누적합을 만들고(k) 배치하는(n) 작업뿐이라 <b>최선·평균·최악 모두 </b>' +
        '<code>O(n+k)</code>다(k = 값의 범위). k가 n에 비해 작으면 비교 정렬의 하한 n log n을 깨고 ' +
        '<b>선형 시간</b>에 정렬한다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>한계:</b> ① count 배열 크기가 <b>값의 범위 k에 비례</b>하므로, <b>k가 매우 크면</b>(예: 0~10억) 메모리·시간이 ' +
        '심하게 낭비된다. ② <b>정수처럼 인덱스로 쓸 수 있는 데이터에만</b> 적합하고, 실수·문자열 등에는 직접 쓰기 ' +
        '어렵다. ③ 보조 배열(count O(k), 출력 O(n))이 필요해 <b>제자리 정렬이 아니다.</b>',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '도수 정렬은 누적 도수를 이용해 <b>같은 키를 입력에 나온 순서대로</b> 배치하면 <b>안정 정렬</b>이 된다(입력을 ' +
        '뒤에서부터 읽고 누적값을 감소시키며 채우는 이유). 같은 키의 순서가 중요한 다단계 정렬(기수 정렬의 ' +
        '내부 단계 등)에서 안정성이 활용된다.',
    },

    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '9. 안정성에 영향을 주는 요인' },
    {
      type: 'p',
      html:
        '안정성은 CPU 속도·운영체제·컴파일러 같은 <b>실행 환경과 무관</b>하다. 오직 <b>알고리즘이 같은 키를 ' +
        '만났을 때 어떻게 비교·교환·처리하는가</b>에 의해 결정된다.',
    },
    {
      type: 'list',
      items: [
        '<b>비교/교환 방식</b> — 같은 키에서 교환을 "하지 않도록"(예: <code>&gt;</code>만, <code>&gt;=</code> 아님) 짜면 안정성이 보존된다.',
        '<b>비교 부호의 등호 처리</b> — merge에서 <code>&lt;=</code>면 안정, <code>&lt;</code>면 불안정해질 수 있다.',
        '<b>교환 거리</b> — 인접 교환(버블·삽입)은 안정, 먼 거리 교환(선택·쉘·퀵·힙)은 불안정의 원인.',
        '<b>안정 ↔ 불안정 전환 가능</b> — 불안정 정렬도 (원래 인덱스를 보조 키로 추가하는 등) 구현을 바꾸면 안정화할 수 있다. 즉 안정성은 "알고리즘 + 구현"의 성질이다.',
      ],
    },
    { type: 'viz', component: 'StabilityViz' },

    // ────────────────────────────────────────────────────────────
    { type: 'h3', text: '10. 정렬 알고리즘 선택 기준' },
    {
      type: 'p',
      html:
        '정렬 선택은 <b>데이터의 크기·분포, 안정성 요구, 메모리 제약</b>을 종합해 결정한다. ' +
        '<b>변수 이름 같은 무관한 요소</b>는 선택 기준이 아니다(대표 함정 보기).',
    },
    {
      type: 'table',
      headers: ['상황 / 요구', '추천 정렬', '이유'],
      rows: [
        ['데이터가 거의 정렬됨', '삽입 정렬', '이동이 거의 없어 O(n)에 근접'],
        ['데이터가 작음(수십 개)', '삽입/버블', '구현 단순, 상수 오버헤드 작음'],
        ['최악에도 O(n log n) 보장 필요', '병합 또는 힙', '입력과 무관하게 O(n log n)'],
        ['안정성이 반드시 필요', '병합 정렬', '안정 + 항상 O(n log n)'],
        ['메모리가 빠듯함', '힙 또는 퀵', '제자리 정렬(보조 배열 불필요)'],
        ['평균 속도 최우선(범용)', '퀵 정렬', '평균 O(n log n), 캐시 친화·상수 작음'],
        ['값 범위 k가 작은 정수', '도수 정렬', '비비교 O(n+k)로 선형 시간'],
      ],
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '<b>요약 암기법:</b> "<b>항상</b> n log n + <b>안정</b> = 병합", "제자리 + 최악 보장 = 힙", "평균 빠른 범용 = 퀵", ' +
        '"<b>거의 정렬</b> = 삽입", "<b>작은 정수 범위</b> = 도수". 이 다섯 매핑이 객관식 정답의 8할을 커버한다.',
    },
  ],
}
