// Ch08 리스트 — 세분화 개념(WF2). ch08-list.js 의 concept 를 교체한다.
// 전공(CS) 수준으로 세분화: 배열 vs 연결 리스트, 노드/포인터 모델, 순차 탐색,
// 삽입·삭제 비용, 단순·이중·원형·원형이중·커서 연결 리스트.
// 허용 viz: LinkedListVisualizer, ListCostViz, CursorListViz

export const concept = {
  summary:
    '리스트(list)는 원소를 일렬로 늘어놓아 순서를 부여한 선형 자료구조다. 같은 추상 자료형(ADT)이라도 ' +
    '구현 방식에 따라 성능이 완전히 달라진다. 연속된 메모리에 담는 배열 리스트는 인덱스 임의 접근이 O(1)인 ' +
    '대신 중간 삽입·삭제 때 원소를 밀어야 하고, 노드를 링크로 잇는 연결 리스트는 임의 접근이 O(n)인 대신 ' +
    '위치만 알면 링크 몇 개만 고쳐 O(1)로 삽입·삭제한다. 연결 리스트는 다시 연결 방향과 끝맺음 방식에 따라 ' +
    '단순·이중·원형·원형이중·커서 리스트로 나뉜다.',
  blocks: [
    // ─────────────────────────────────────────────────────────────
    { type: 'h3', text: '1. 리스트란 무엇인가 — ADT와 구현의 분리' },
    {
      type: 'p',
      html:
        '<b>리스트(list)</b>는 0개 이상의 원소를 일렬로 늘어놓아 <b>순서(앞·뒤)</b>를 부여한 선형 자료구조다. ' +
        '“첫 원소”, “마지막 원소”, “어떤 원소의 바로 다음 원소”가 정의된다는 점이 핵심이다. ' +
        '리스트는 우리가 다루는 <b>추상 자료형(ADT)</b>이고, 그것을 메모리에 어떻게 올리느냐가 <b>구현</b>이다. ' +
        '같은 “리스트”라도 <b>배열 기반 구현</b>과 <b>연결(linked) 구현</b>은 연산별 시간복잡도가 다르므로, ' +
        '시험에서는 “어떤 연산이 무엇을 잘하고 무엇을 못하는가”를 구현별로 구분해 묻는다.',
    },
    {
      type: 'list',
      items: [
        '<b>접근(access)</b> — i번째 원소를 읽는다. 배열은 주소 계산만으로 O(1), 연결 리스트는 앞에서부터 따라가 O(n).',
        '<b>탐색(search)</b> — 특정 값을 가진 원소를 찾는다. 정렬·구조에 따라 순차 또는 이진.',
        '<b>삽입(insert) / 삭제(delete)</b> — 특정 위치에 원소를 끼우거나 뺀다. 비용의 차이가 가장 크게 갈리는 지점.',
      ],
    },

    // ─────────────────────────────────────────────────────────────
    { type: 'h3', text: '2. 배열 리스트 vs 연결 리스트' },
    {
      type: 'p',
      html:
        '<b>배열 리스트</b>는 원소들을 <b>연속된 메모리</b>에 차곡차곡 담는다. i번째 원소의 주소는 ' +
        '<code>기준주소 + i × 원소크기</code>로 한 번에 계산되므로 <b>임의 접근(random access)</b>이 O(1)이다. ' +
        '대신 중간에 끼우거나 빼면 그 뒤의 원소를 한 칸씩 <b>이동(shift)</b>해야 해서 최악 O(n)이다.<br>' +
        '<b>연결 리스트</b>는 원소를 담은 <b>노드(node)</b>를 메모리 곳곳에 흩어 두고 <b>포인터(링크)</b>로 잇는다. ' +
        'i번째에 가려면 head부터 i번 따라가야 하므로 접근·탐색이 O(n)이지만, 끼울·뺄 <b>위치를 이미 알고 있다면</b> ' +
        '링크 몇 개만 고쳐 O(1)로 삽입·삭제한다(데이터를 밀 필요가 없다).',
    },
    {
      type: 'table',
      headers: ['연산', '배열 리스트', '연결 리스트(단순)', '설명'],
      rows: [
        ['i번째 임의 접근', '<b>O(1)</b>', 'O(n)', '배열은 주소 계산 1회, 연결은 head부터 i번 추적'],
        ['값 탐색(search)', 'O(n) / 정렬 시 O(log n)', 'O(n)', '연결 리스트는 임의 접근이 안 돼 이진 탐색 부적합'],
        ['맨 앞 삽입·삭제', 'O(n) (전체 이동)', '<b>O(1)</b>', 'head만 바꾸면 끝'],
        ['맨 뒤 삽입', 'O(1)* (자리 있을 때)', 'tail 있으면 O(1), 없으면 O(n)', '*가득 차면 재할당 O(n)'],
        ['맨 뒤 삭제', 'O(1)', 'O(n)', '단순 리스트는 tail의 이전 노드를 못 바로 찾음'],
        ['중간 삽입·삭제(위치 알 때)', 'O(n) (shift)', '<b>O(1)</b>', '연결은 링크만 조정'],
        ['메모리 형태', '연속 공간', '노드마다 링크(포인터) 추가', '연결은 포인터 저장 공간 오버헤드'],
        ['크기 변경', '재할당 필요(동적 배열)', '노드 추가/제거로 자유로움', '연결은 미리 크기 정할 필요 없음'],
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '“연결 리스트가 배열보다 항상 삽입·삭제가 빠르다”는 <b>오개념</b>이다. 링크 조정 자체는 O(1)이지만, ' +
        '<b>삽입·삭제할 위치(또는 그 이전 노드)를 찾는 탐색</b>이 O(n)일 수 있다. 정확히는 “<u>위치를 이미 알고 있을 때</u> ' +
        '데이터 이동 없이 O(1)”이다. 또 연결 리스트는 인덱스 임의 접근이 느리므로 <b>이진 탐색에 부적합</b>하다.',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '암기 대신 한 문장으로: <b>배열은 “읽기에 강하고 고치기에 약하다”, 연결 리스트는 “고치기에 강하고 읽기에 약하다.”</b> ' +
        '아래 시각화로 연산별 비용 차이를 직접 비교해 보세요.',
    },
    { type: 'viz', component: 'ListCostViz' },

    // ─────────────────────────────────────────────────────────────
    { type: 'h3', text: '3. 연결 리스트의 구성 요소' },

    { type: 'h4', text: '3.1 노드(node): 데이터 + next' },
    {
      type: 'p',
      html:
        '연결 리스트의 기본 단위는 <b>노드</b>다. 단순 연결 리스트의 노드는 두 칸을 가진다: ' +
        '실제 값을 담는 <b>데이터(data)</b> 필드와, 다음 노드를 가리키는 <b>링크 포인터 <code>next</code></b>다. ' +
        '노드는 “값 + 다음으로 가는 화살표”라고 생각하면 된다.',
    },
    {
      type: 'p',
      html:
        '<code>class Node:</code><br>' +
        '&nbsp;&nbsp;<code>def __init__(self, data):</code><br>' +
        '&nbsp;&nbsp;&nbsp;&nbsp;<code>self.data = data&nbsp;&nbsp;# 저장할 값</code><br>' +
        '&nbsp;&nbsp;&nbsp;&nbsp;<code>self.next = None&nbsp;&nbsp;# 다음 노드(없으면 None)</code>',
    },

    { type: 'h4', text: '3.2 head · tail · current' },
    {
      type: 'list',
      items: [
        '<b>head</b> — 리스트의 <b>첫 노드</b>를 가리키는 진입점. 리스트는 보통 head 하나만으로 표현된다. ' +
          '리스트가 <b>비어 있으면 head는 <code>None</code></b>이다.',
        '<b>tail</b> — 리스트의 <b>마지막 노드</b>. 단순 연결 리스트에서 <b>tail의 next는 <code>None</code></b>이다(자기 자신이 아니다). ' +
          'tail 포인터를 따로 들고 있으면 맨 뒤 “삽입”은 O(1)로 빨라진다.',
        '<b>current</b> — 순회·탐색 중 <b>지금 주목하고 있는 노드</b>를 가리키는 작업용 포인터. ' +
          '한 칸 전진하는 동작(<code>next()</code>)은 <code>current = current.next</code>로 구현한다.',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '노드가 <b>하나뿐</b>이면 그 노드는 <b>head이면서 동시에 tail</b>이다. 또 “tail이 있으면 맨 뒤 ‘삽입’은 O(1)”이지만 ' +
        '단순 연결 리스트에서 맨 뒤 ‘<b>삭제</b>’는 tail만으로 해결되지 않는다(이유는 4.3 참고).',
    },

    { type: 'h4', text: '3.3 predecessor(선행) · successor(후속)' },
    {
      type: 'p',
      html:
        '어떤 노드 X를 기준으로, X 바로 <b>앞</b>의 노드를 <b>predecessor(선행 노드)</b>, 바로 <b>뒤</b>의 노드를 ' +
        '<b>successor(후속 노드)</b>라 한다. 트리의 부모/자식과 혼동하지 말 것 — 리스트의 선행/후속은 ' +
        '<b>같은 일렬 위에서의 한 칸 앞·뒤</b>일 뿐이다.',
    },
    {
      type: 'table',
      headers: ['용어', '의미', '단순 리스트에서 찾는 비용'],
      rows: [
        ['successor(후속)', 'X 바로 뒤 노드 = <code>X.next</code>', 'O(1) — next 한 번'],
        ['predecessor(선행)', 'X 바로 앞 노드', 'O(n) — head부터 “next가 X인 노드”를 순차 탐색'],
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '단순 연결 리스트의 핵심 약점: <b>successor는 즉시 알지만 predecessor는 즉시 알 수 없다.</b> ' +
        'next 링크가 한 방향이라 “앞으로 거슬러” 갈 수 없기 때문이다. 이 비대칭이 4장의 삭제 비용 차이를 만든다. ' +
        '(이중 연결 리스트는 <code>prev</code> 링크로 이 문제를 없앤다.)',
    },

    // ─────────────────────────────────────────────────────────────
    { type: 'h3', text: '4. 핵심 연산과 비용' },

    { type: 'h4', text: '4.1 순차 탐색 search() — O(n)' },
    {
      type: 'p',
      html:
        '연결 리스트는 임의 접근이 불가능하므로, 값을 찾으려면 <code>current = head</code>에서 시작해 ' +
        '<code>current = current.next</code>로 한 칸씩 이동하며 값을 비교하는 <b>순차 탐색(linear/sequential search)</b>을 쓴다. ' +
        '찾으면 그 노드를, 끝(<code>None</code>)까지 못 찾으면 실패를 반환한다. 평균·최악 <b>O(n)</b>이다. ' +
        '정렬되어 있어도 <b>이진 탐색은 쓸 수 없다</b> — 중간 원소로 한 번에 점프하는 임의 접근이 O(n)이라 이점이 사라진다.',
    },

    { type: 'h4', text: '4.2 삽입 insert' },
    {
      type: 'list',
      items: [
        '<b>맨 앞 삽입(prepend)</b> — 새 노드의 next를 기존 head로, head를 새 노드로. <b>O(1)</b>.',
        '<b>중간 삽입</b> — 끼울 위치의 <u>이전 노드(predecessor) p</u>를 알고 있다면 ' +
          '<code>new.next = p.next; p.next = new</code> 두 줄. <b>O(1)</b>. 다만 그 위치를 찾는 데 O(n)이 들 수 있다.',
        '<b>맨 뒤 삽입(append)</b> — tail 포인터가 있으면 O(1), 없으면 끝까지 가야 해서 O(n).',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '중간 삽입에서 링크를 잇는 <b>순서</b>가 중요하다. 반드시 <code>new.next = p.next</code>를 먼저 한 뒤 ' +
        '<code>p.next = new</code>로 해야 한다. 순서를 뒤집어 <code>p.next = new</code>를 먼저 하면 ' +
        '<b>원래 뒷부분으로 가는 링크를 잃어버려</b> 리스트가 끊긴다.',
    },

    { type: 'h4', text: '4.3 삭제 delete — remove_first vs remove_last' },
    {
      type: 'p',
      html:
        '삭제 비용은 “<b>지울 노드의 predecessor를 얼마나 쉽게 아느냐</b>”로 갈린다. 단순 연결 리스트에서 노드를 빼려면 ' +
        '그 <b>이전 노드의 next를 지울 노드의 next로</b> 건너뛰게 만들어야 하는데, 이전 노드를 알아야 하기 때문이다.',
    },
    {
      type: 'table',
      headers: ['연산', '하는 일', '시간복잡도', '이유'],
      rows: [
        ['<code>remove_first()</code>', '<code>head = head.next</code> 로 첫 노드 건너뛰기',
          '<b>O(1)</b>', '첫 노드의 predecessor는 없고 head만 옮기면 됨'],
        ['<code>remove_last()</code>', 'tail의 <b>이전(predecessor)</b> 노드를 찾아 그 next를 <code>None</code>으로',
          '<b>O(n)</b>', '단순 리스트는 끝 노드의 이전 노드를 바로 알 수 없어 head부터 순차 탐색'],
        ['중간 삭제(위치 알 때)', 'predecessor.next = 지울노드.next',
          'O(1)', '이전 노드를 이미 알고 있다는 전제'],
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>tail 포인터가 있어도</b> 단순 연결 리스트의 <code>remove_last()</code>는 O(1)이 되지 않는다. ' +
        '삭제 후 tail을 “이전 노드”로 옮겨야 하는데, 그 이전 노드를 찾으려면 결국 head부터 순차 탐색해야 하기 때문이다. ' +
        '반면 <code>remove_first()</code>는 항상 O(1)이다. (이중 연결 리스트라면 <code>tail.prev</code>로 O(1) 삭제가 가능.)',
    },

    { type: 'h4', text: '4.4 전체 비우기 clear()' },
    {
      type: 'p',
      html:
        '모든 노드를 제거하는 <code>clear()</code>는 보통 <b>head에서 시작해 앞에서부터</b> 차례로 노드를 떼어낸다 ' +
        '(가장 간단한 구현은 <code>head = None</code>으로 진입점을 끊는 것). “tail부터 거꾸로 반복 삭제한다”는 ' +
        '설명은 단순 연결 리스트에서 자연스럽지 않다 — 거꾸로 가는 링크가 없기 때문이다.',
    },

    { type: 'h4', text: '4.5 동작을 눈으로 — LinkedListVisualizer' },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '아래에서 노드를 직접 삽입·삭제하며 head/current 포인터와 next 링크가 단계별로 어떻게 바뀌는지 확인하세요. ' +
        '특히 “중간 삽입의 링크 연결 순서”와 “remove_last가 이전 노드를 찾아가는 과정”을 눈으로 따라가 보면 4장이 단단해집니다.',
    },
    { type: 'viz', component: 'LinkedListVisualizer' },

    // ─────────────────────────────────────────────────────────────
    { type: 'h3', text: '5. 연결 리스트의 종류' },
    {
      type: 'p',
      html:
        '연결 방식(한 방향/양방향), 끝맺음 방식(<code>None</code>으로 끝/원형으로 순환), 더미 노드 사용 여부, ' +
        '그리고 링크를 포인터로 할지 배열 인덱스로 할지에 따라 다섯 가지로 나뉜다.',
    },

    { type: 'h4', text: '5.1 단순 연결 리스트(singly linked list)' },
    {
      type: 'list',
      items: [
        '각 노드가 <code>next</code> <b>하나</b>만 가지는, 한 방향 리스트.',
        '마지막 노드의 <code>next</code>는 <b><code>None</code></b>(자기 자신이 아님).',
        '구조가 단순하고 메모리 오버헤드가 작지만, predecessor를 못 거슬러 가 ' +
          '<code>remove_last()</code>가 O(n)인 등 “뒤쪽·역방향” 작업에 약하다.',
      ],
    },

    { type: 'h4', text: '5.2 이중 연결 리스트(doubly linked list)' },
    {
      type: 'list',
      items: [
        '각 노드가 <b><code>prev</code>(이전)와 <code>next</code>(다음)</b> 두 링크를 가져 <b>양방향 탐색</b>이 가능.',
        '어떤 노드의 predecessor를 <code>node.prev</code>로 <b>즉시</b> 알 수 있어, 그 노드의 삭제가 O(1)이고 ' +
          '<code>tail.prev</code>로 <code>remove_last()</code>도 O(1)이 된다.',
        '대가로 노드마다 링크를 하나 더 저장해 <b>메모리를 더 쓰고</b>, 삽입·삭제 때 고쳐야 할 링크가 늘어난다 ' +
          '(prev·next 양쪽을 모두 갱신).',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '이중 연결 리스트의 장점은 “양방향 탐색·역방향 작업”이지 “검색이 빨라지는 것”이 아니다. ' +
        '값 탐색은 여전히 순차 <b>O(n)</b>이며, prev가 있다고 임의 접근이 O(1)이 되지도 않는다.',
    },

    { type: 'h4', text: '5.3 원형 연결 리스트(circular linked list)' },
    {
      type: 'list',
      items: [
        '마지막 노드의 next가 <code>None</code> 대신 <b>head를 가리켜</b> 끝과 처음이 이어진다(<code>tail.next = head</code>).',
        '어느 노드에서 출발해도 한 바퀴 돌아 모든 노드에 닿을 수 있어, 라운드로빈·순환 버퍼 같은 “돌아가며 처리” 작업에 적합.',
        '순회 종료 조건이 “<code>next == None</code>”이 아니라 “<b>다시 head(시작 노드)로 돌아오면</b>”이 된다.',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '원형 리스트에서 “<code>next == None</code>이면 끝”이라는 단순 리스트식 종료 조건을 그대로 쓰면 ' +
        '<code>None</code>을 영영 만나지 못해 <b>무한 루프</b>에 빠진다. 시작 노드로 되돌아왔는지로 종료를 판정해야 한다.',
    },

    { type: 'h4', text: '5.4 원형 이중 연결 리스트 + 더미(dummy) 노드' },
    {
      type: 'list',
      items: [
        '<b>원형 + 이중</b>의 결합: 각 노드가 prev·next를 갖고, 양 끝이 서로 이어져 앞뒤 어느 방향으로도 끝없이 순환한다.',
        '여기에 값 없는 <b>더미(헤더/센티넬) 노드</b>를 하나 두면, 빈 리스트도 “더미 혼자 자기 자신을 가리키는” 항상-유효한 형태가 된다.',
        '더미 덕분에 “리스트가 비었는가/맨 앞·맨 뒤인가” 같은 <b>경계(특수) 조건이 사라져</b> 삽입·삭제 코드가 한 가지 일반 케이스로 단순해진다.',
      ],
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '더미 노드의 본질은 “예외 케이스 제거”다. 더미가 항상 존재하므로 어떤 위치든 삽입·삭제가 ' +
        '“새 노드를 두 노드 사이에 끼우고 양쪽 prev/next를 잇는다”는 <b>동일한 절차</b>로 처리되어 if 분기가 줄어든다.',
    },

    { type: 'h4', text: '5.5 커서(cursor) 연결 리스트와 free list' },
    {
      type: 'p',
      html:
        '<b>커서 연결 리스트</b>는 진짜 포인터(메모리 주소) 대신 <b>배열의 인덱스</b>를 링크로 사용하는 구현이다. ' +
        '노드들을 하나의 배열에 담고, 각 칸은 <code>data</code>와 “다음 노드가 있는 배열 인덱스(<code>next</code>)”를 저장한다. ' +
        '포인터를 직접 못 쓰는 환경이나, 모든 노드를 한 배열에 모아 관리하고 싶을 때 쓴다(파일·DB 레코드 연결 등).',
    },
    {
      type: 'list',
      items: [
        '<b>링크 = 배열 인덱스</b> — “다음으로 가라”는 곧 “이 인덱스 칸으로 가라”는 뜻. 리스트의 끝은 보통 ' +
          '-1 같은 특수 인덱스로 표시한다(포인터의 <code>None</code> 대응).',
        '<b>free list(자유 공간 리스트)</b> — 현재 비어 있는(삭제됐거나 한 번도 안 쓴) 배열 칸들을 하나의 연결 리스트로 묶어 관리한다. ' +
          '삽입 때는 free list에서 빈 칸을 하나 꺼내 쓰고, 삭제 때는 그 칸을 다시 free list로 되돌려 <b>재사용</b>한다.',
      ],
    },
    {
      type: 'table',
      headers: ['포인터 방식', '커서 방식', '대응'],
      rows: [
        ['노드 = 동적 할당 객체', '노드 = 고정 배열의 한 칸', '메모리를 배열로 직접 관리'],
        ['link = 메모리 주소', 'link = 배열 인덱스', '“다음”을 주소 대신 첨자로'],
        ['끝 표시 = <code>None</code>', '끝 표시 = -1(약속된 인덱스)', '널 링크의 표현'],
        ['new/malloc, free/del', 'free list에서 꺼내 쓰고 되돌림', '빈 칸 재활용'],
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '커서 리스트에서 <b>삭제만 반복하고 free list로 되돌려 재사용하지 않으면 빈(쓰지 못하는) 레코드가 점점 쌓인다.</b> ' +
        '그래서 free list로 삭제된 칸을 회수해 다음 삽입에 재활용하는 것이 핵심이다.',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '아래 시각화로 “인덱스가 곧 링크”라는 개념과, 삭제된 칸이 free list로 회수돼 다음 삽입에서 재사용되는 흐름을 따라가 보세요.',
    },
    { type: 'viz', component: 'CursorListViz' },

    // ─────────────────────────────────────────────────────────────
    { type: 'h3', text: '6. 종류별 한눈 비교' },
    {
      type: 'table',
      headers: ['종류', '링크', '끝맺음', '핵심 장점', '대표 약점'],
      rows: [
        ['단순', '<code>next</code>만', 'tail.next = <code>None</code>', '단순·저메모리', 'predecessor 추적 불가 → remove_last O(n)'],
        ['이중', '<code>prev</code> + <code>next</code>', '양 끝 <code>None</code>', '양방향 탐색, 임의 노드/맨뒤 삭제 O(1)', '링크 추가로 메모리·갱신 비용↑'],
        ['원형', '<code>next</code>(보통)', 'tail.next = head', '순환 처리·라운드로빈에 적합', '종료 조건 주의(무한 루프)'],
        ['원형 이중', '<code>prev</code> + <code>next</code>', '양 끝이 순환', '더미 노드로 경계 조건 제거', '구현이 가장 복잡, 메모리↑'],
        ['커서', '배열 인덱스', '끝 = -1 약속', '포인터 없이 배열로 관리, 노드 한곳 집중', 'free list 관리 필요, 빈 칸 누적 위험'],
      ],
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '시험 대비 요약 3줄 — ① <b>임의 접근</b>: 배열 O(1) / 연결 O(n), 그래서 연결 리스트는 이진 탐색 부적합. ' +
        '② <b>remove_first O(1)</b> vs <b>단순 리스트 remove_last O(n)</b>(predecessor를 못 바로 알아서). ' +
        '③ <b>이중</b>=양방향, <b>원형</b>=tail.next가 head, <b>원형이중+더미</b>=경계 조건 제거, <b>커서</b>=인덱스 링크+free list 재사용.',
    },
  ],
}
