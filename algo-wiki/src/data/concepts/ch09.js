// Ch09 트리 — 개념 정리(심화). WF2가 채우는 concept 객체.
// 블록 렌더러: src/components/ConceptSection.jsx
// 사용 가능한 viz: BSTVisualizer, HeapArrayViz, TraversalCompareViz, BSTShapeViz

export const concept = {
  summary:
    '트리(tree)는 노드를 부모-자식 관계로 잇는 비선형 계층 자료구조다. 하나의 루트에서 시작해 ' +
    '사이클 없이 가지를 뻗으며, n개의 노드는 정확히 n-1개의 간선으로 연결된다. 이진 트리는 각 노드가 ' +
    '최대 두 자식을 갖는 특수 트리이고, 순회(전위·중위·후위=DFS, 레벨=BFS)로 모든 노드를 체계적으로 ' +
    '방문한다. 완전 이진 트리는 배열로 빈틈없이 표현되어 힙의 토대가 되고, 이진 검색 트리(BST)는 ' +
    '"왼쪽 < 부모 < 오른쪽" 규칙으로 탐색·삽입·삭제를 균형 시 O(log n)에 처리한다.',
  blocks: [
    // ───────────────────────────────────────────── 1. 트리의 정의와 용어
    { type: 'h3', text: '1. 트리란 무엇인가' },
    {
      type: 'p',
      html:
        '<b>트리(tree)</b>는 데이터를 <b>계층적(부모-자식) 관계</b>로 표현하는 <b>비선형(non-linear)</b> ' +
        '자료구조다. 배열이나 연결 리스트가 한 줄로 늘어선 선형 구조라면, 트리는 한 노드 아래로 여러 ' +
        '갈래가 뻗어 나간다. 파일 시스템의 폴더 구조, 조직도, HTML DOM, 표현식 파싱 결과가 모두 트리다.',
    },
    {
      type: 'list',
      items: [
        '<b>단 하나의 루트</b>에서 시작한다. 루트는 부모가 없는 유일한 노드다.',
        '루트를 제외한 모든 노드는 <b>정확히 하나의 부모</b>를 가진다.',
        '<b>사이클(순환)이 없다.</b> 어떤 노드에서 출발해 다시 자기 자신으로 돌아오는 경로가 없다.',
        '노드가 <code>n</code>개이면 간선(edge)은 항상 <code>n-1</code>개다(루트만 부모로 가는 간선이 없으므로).',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '"모든 노드는 반드시 부모를 가진다"는 <b>틀린</b> 설명이다. <b>루트는 부모가 없다.</b> ' +
        '루트를 제외한 나머지 노드만 부모를 하나씩 갖는다. 시험 단골 함정이다.',
    },

    { type: 'h4', text: '1-1. 핵심 용어 정리' },
    {
      type: 'table',
      headers: ['용어', '정의', '비고'],
      rows: [
        ['루트(root)', '부모가 없는 최상위 노드', '트리에 단 하나만 존재'],
        ['리프(leaf, 단말)', '자식이 없는 노드', '차수(degree)가 0인 노드'],
        ['내부 노드(internal)', '자식이 하나 이상 있는 노드', '리프가 아닌 노드'],
        ['부모/자식(parent/child)', '바로 위/아래로 연결된 노드', '간선 하나로 직접 연결'],
        ['형제(sibling)', '<b>부모가 같은</b> 노드들', '같은 레벨이라도 부모가 다르면 형제가 아님'],
        ['조상/후손(ancestor/descendant)', '위쪽/아래쪽 경로상의 모든 노드', '부모의 부모, 자식의 자식 …'],
        ['서브트리(subtree)', '어떤 노드와 그 후손 전체가 이루는 트리', '재귀 정의의 기반'],
      ],
    },

    { type: 'h4', text: '1-2. 차수(degree) · 레벨(level) · 깊이(depth) · 높이(height)' },
    {
      type: 'list',
      items: [
        '<b>차수(degree)</b> — 한 <b>노드가 가진 자식의 수</b>. 트리의 차수는 모든 노드 차수의 최댓값. (이진 트리는 모든 노드의 차수가 0·1·2 중 하나.)',
        '<b>레벨(level)</b> — 루트를 레벨 0(또는 1)으로 두고 한 단계 내려갈 때마다 1씩 증가. 같은 레벨이라고 형제는 아니다.',
        '<b>깊이(depth)</b> — 특정 노드에서 <b>루트까지</b>의 간선 수. 루트의 깊이는 0.',
        '<b>높이(height)</b> — <b>루트에서 가장 먼 리프까지의 거리(가장 긴 경로의 간선 수)</b>. 트리 전체의 높이 = 루트의 높이.',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>높이는 "노드 개수"가 아니다.</b> 높이는 루트에서 가장 깊은 리프까지의 <b>경로 길이(거리)</b>다. ' +
        '또한 "전체 노드 수", "루트의 차수", "리프 개수" 중 어느 것도 높이가 아니다. ' +
        '노드 하나뿐인 트리의 높이는 0이다.',
    },

    // ───────────────────────────────────────────── 2. 이진 트리
    { type: 'h3', text: '2. 이진 트리(binary tree)' },
    {
      type: 'p',
      html:
        '<b>이진 트리</b>는 모든 노드가 <b>최대 두 개</b>의 자식(왼쪽 자식·오른쪽 자식)을 갖는 트리다. ' +
        '자식이 0개·1개·2개일 수 있으며, 왼쪽과 오른쪽은 <b>구분된다</b>(자식이 하나여도 그것이 왼쪽인지 ' +
        '오른쪽인지가 의미를 가진다). 트리·순회·BST·힙 모두 이 이진 트리를 기반으로 한다.',
    },
    {
      type: 'table',
      headers: ['종류', '조건', '특징'],
      rows: [
        ['정 이진 트리(full)', '모든 노드가 자식 0개 또는 2개', '자식이 1개인 노드가 없음'],
        ['완전 이진 트리(complete)', '마지막 레벨 빼고 꽉 차고, 마지막은 왼쪽부터 채움', '배열로 빈틈없이 표현 → 힙'],
        ['포화 이진 트리(perfect)', '모든 레벨이 완전히 꽉 참', '리프가 모두 같은 레벨, 노드 수 = 2^(h+1)-1'],
        ['편향 트리(skewed)', '모든 노드가 한쪽 자식만 가짐', '사실상 연결 리스트, 높이 = n-1'],
      ],
    },

    // ───────────────────────────────────────────── 3. 순회
    { type: 'h3', text: '3. 트리 순회(traversal)' },
    {
      type: 'p',
      html:
        '<b>순회</b>는 트리의 모든 노드를 <b>빠짐없이 한 번씩</b> 정해진 규칙으로 방문하는 것이다. ' +
        '크게 <b>깊이 우선(DFS)</b>과 <b>너비 우선(BFS)</b>으로 나뉜다. DFS는 다시 ' +
        '현재 노드를 언제 방문하느냐에 따라 <b>전위·중위·후위</b>로 갈린다.',
    },

    { type: 'h4', text: '3-1. DFS vs BFS' },
    {
      type: 'list',
      items: [
        '<b>DFS(깊이 우선 탐색, Depth-First Search)</b> — 한 경로를 <b>리프까지 깊게</b> 내려갔다가 막히면 되돌아와 다른 가지로 간다. <b>전위·중위·후위</b>가 모두 DFS다. 보통 재귀(또는 스택)로 구현한다.',
        '<b>BFS(너비 우선 탐색, Breadth-First Search)</b> — <b>같은 레벨</b>을 모두 방문한 뒤 다음 레벨로 내려간다. <b>레벨 순회(level order)</b>가 BFS다. <b>큐(queue)</b>로 구현한다.',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>레벨 순회(level order)는 DFS가 아니라 BFS다.</b> "DFS에 해당하지 않는 순회"를 묻는 문제의 ' +
        '정답은 항상 level order다. 전위·중위·후위만 DFS임을 기억하자.',
    },

    { type: 'h4', text: '3-2. 세 가지 DFS 순회 — 방문 순서' },
    {
      type: 'p',
      html:
        '세 순회의 차이는 <b>현재 노드(N)를 왼쪽 서브트리(L)·오른쪽 서브트리(R)에 대해 언제 찍느냐</b> ' +
        '하나뿐이다. 왼쪽을 오른쪽보다 먼저 보는 것은 셋 다 공통이다.',
    },
    {
      type: 'table',
      headers: ['순회', '방문 순서', '현재 노드 위치', '분류'],
      rows: [
        ['전위(preorder)', '<b>노드 → 왼쪽 → 오른쪽</b> (N L R)', '맨 처음', 'DFS'],
        ['중위(inorder)', '<b>왼쪽 → 노드 → 오른쪽</b> (L N R)', '가운데', 'DFS'],
        ['후위(postorder)', '<b>왼쪽 → 오른쪽 → 노드</b> (L R N)', '맨 마지막', 'DFS'],
        ['레벨(level order)', '<b>위 레벨부터 좌→우</b>', '레벨 단위', 'BFS'],
      ],
    },
    {
      type: 'list',
      items: [
        '<b>전위</b>는 현재 노드를 <b>가장 먼저</b> 방문한다 → 트리를 복사하거나 구조를 위에서부터 출력할 때.',
        '<b>중위</b>는 현재 노드를 <b>가운데</b> 방문한다 → <b>BST에서 오름차순 정렬</b> 결과를 얻을 때.',
        '<b>후위</b>는 현재 노드를 <b>가장 마지막</b>에 방문한다 → 자식을 먼저 처리해야 하는 경우(서브트리 삭제, 디렉터리 용량 계산, 후위 표기식).',
      ],
    },
    {
      type: 'p',
      html:
        '<b>예시.</b> 루트 8, 왼쪽 3, 오른쪽 10, 10의 오른쪽 14인 트리에서 — ' +
        '전위: <code>8 3 10 14</code> / 중위: <code>3 8 10 14</code> / 후위: <code>3 14 10 8</code> / ' +
        '레벨: <code>8 3 10 14</code>. 중위 결과가 오름차순임에 주목.',
    },
    { type: 'viz', component: 'TraversalCompareViz' },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '위 시각화에서 같은 트리를 전위·중위·후위·레벨로 동시에 순회하며 방문 순서가 어떻게 ' +
        '달라지는지 비교해 보세요. 손으로는 "현재 노드를 언제 찍는가(N의 위치)"만 바꿔가며 따라 적으면 됩니다.',
    },

    // ───────────────────────────────────────────── 4. 완전 이진 트리와 배열 표현
    { type: 'h3', text: '4. 완전 이진 트리와 배열 표현' },
    {
      type: 'p',
      html:
        '<b>완전 이진 트리(complete binary tree)</b>는 <b>마지막 레벨을 제외한 모든 레벨이 꽉 차 있고</b>, ' +
        '<b>마지막 레벨은 왼쪽부터 빈틈없이 채워진</b> 이진 트리다. 마지막 레벨이 끝까지 꽉 찰 필요는 없지만, ' +
        '중간에 구멍이 생기거나 오른쪽부터 채워서는 안 된다. 이 "왼쪽부터 빈틈없음" 덕분에 <b>배열에 인덱스 ' +
        '순서대로</b> 담을 수 있고, 힙(heap)이 바로 이 표현을 쓴다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '오개념 정리 — ① 완전 이진 트리의 <b>마지막 레벨이 반드시 꽉 찰 필요는 없다</b>(꽉 차면 포화 이진 트리). ' +
        '② 채우는 방향은 <b>왼쪽부터</b>(오른쪽부터·임의 순서 X). ' +
        '③ "모든 노드가 반드시 두 자식을 가진다"는 조건은 완전 이진 트리가 아니라 정/포화 트리 쪽 이야기다.',
    },

    { type: 'h4', text: '4-1. 0-기반 인덱스 매핑 공식' },
    {
      type: 'p',
      html:
        '배열의 인덱스 0을 루트로 두고 레벨 순서(위에서 아래로, 왼쪽에서 오른쪽으로)로 노드를 담으면, ' +
        '인덱스 <code>i</code> 노드의 친척을 <b>계산만으로</b> 찾을 수 있다(포인터가 필요 없다).',
    },
    {
      type: 'table',
      headers: ['관계', '인덱스 공식(0-기반)', '예: i=0(루트)', '예: i=1'],
      rows: [
        ['왼쪽 자식', '<code>2i + 1</code>', '1', '3'],
        ['오른쪽 자식', '<code>2i + 2</code>', '2', '4'],
        ['부모', '<code>(i - 1) // 2</code> (정수 나눗셈)', '루트라 없음', '0'],
      ],
    },
    {
      type: 'p',
      html:
        '예를 들어 배열 <code>[A, B, C, D, E, F, G]</code>(인덱스 0~6)는 루트 A, A의 자식 B·C, ' +
        'B의 자식 D·E, C의 자식 F·G인 포화 이진 트리다. ' +
        'B(i=1)의 왼쪽 자식은 <code>2·1+1=3</code>(D), 오른쪽은 <code>2·1+2=4</code>(E), ' +
        '부모는 <code>(1-1)//2=0</code>(A)로 정확히 맞는다.',
    },
    { type: 'viz', component: 'HeapArrayViz' },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '위 시각화로 배열 인덱스와 트리 위치가 <code>2i+1</code>·<code>2i+2</code>·<code>(i-1)/2</code> ' +
        '공식으로 어떻게 대응되는지 직접 확인하세요. (참고: 1-기반 인덱스를 쓰면 공식이 ' +
        '<code>2i</code>·<code>2i+1</code>·<code>i//2</code>로 바뀐다. 시험은 보통 0-기반을 묻는다.)',
    },

    // ───────────────────────────────────────────── 5. 이진 검색 트리
    { type: 'h3', text: '5. 이진 검색 트리(BST)' },
    {
      type: 'p',
      html:
        '<b>이진 검색 트리(Binary Search Tree)</b>는 정렬 성질을 가진 이진 트리다. ' +
        '모든 노드에 대해 다음이 성립한다 — ' +
        '<b>왼쪽 서브트리의 모든 키 &lt; 그 노드의 키 &lt; 오른쪽 서브트리의 모든 키</b>. ' +
        '한 마디로 "<b>왼쪽은 작고, 오른쪽은 크다</b>". 이 규칙 덕분에 정렬·탐색을 매우 빠르게 처리한다.',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        'BST 조건은 <b>직속 자식뿐 아니라 서브트리 "전체"</b>에 적용된다. ' +
        '왼쪽 자식 하나만 부모보다 작으면 되는 게 아니라, 왼쪽 서브트리에 속한 <b>모든 후손</b>이 ' +
        '부모보다 작아야 한다. "왼쪽이 부모보다 크다 / 오른쪽이 부모보다 작다"는 조건은 모두 BST가 아니다.',
    },

    { type: 'h4', text: '5-1. 중위 순회 = 오름차순 정렬' },
    {
      type: 'p',
      html:
        'BST를 <b>중위 순회(L → N → R)</b>하면 키가 항상 <b>오름차순</b>으로 출력된다. ' +
        '왼쪽(작은 값)을 먼저, 현재 노드를 가운데, 오른쪽(큰 값)을 나중에 방문하니 자연히 정렬된다. ' +
        '이것이 BST의 대표적 장점이다(별도 정렬 없이 정렬된 순서를 뽑아낼 수 있다).',
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        '<b>중위 순회 결과만으로는 트리 모양(루트)을 하나로 특정할 수 없다.</b> 예컨대 중위 결과가 ' +
        '<code>[1, 3, 5, 7, 9]</code>라면 이 정렬 수열을 만드는 BST는 여러 개이고, 따라서 ' +
        '<b>어떤 값이든 루트가 될 수 있다</b>. 같은 값 집합도 삽입 순서에 따라 모양이 달라진다.',
    },

    { type: 'h4', text: '5-2. 탐색(search)과 삽입(insert)' },
    {
      type: 'p',
      html:
        'BST의 탐색·삽입은 <b>루트에서 시작</b>해 찾는 키와 현재 노드를 비교하고, ' +
        '<b>작으면 왼쪽 / 크면 오른쪽</b>으로 <b>한 방향씩만</b> 내려간다. ' +
        '매 단계마다 후보의 절반을 버리므로 이진 탐색과 같은 원리다.',
    },
    {
      type: 'list',
      items: [
        '<b>탐색</b> — 루트와 비교 → 같으면 발견, 작으면 왼쪽으로, 크면 오른쪽으로. 리프를 지나 빈 자리에 도달하면 "없음".',
        '<b>삽입</b> — 탐색과 똑같이 내려가다가 <b>빈 자리(없는 자식)에 도달하면 거기에 새 노드를 단다.</b> 항상 리프 위치에 매달리며, 기존 노드의 값을 바꾸지 않는다.',
        '<b>한 방향 원칙</b> — 양쪽을 동시에 보지 않는다. 비교 결과에 따라 왼쪽 "또는" 오른쪽 하나만 따라간다.',
        '<b>루트는 첫 삽입값</b> — 처음 넣은 값이 루트가 되고, 이후 값은 모두 그 아래로 들어간다.',
      ],
    },
    {
      type: 'p',
      html:
        '<b>탐색 예시.</b> 루트 10, 왼쪽 5, 5의 오른쪽 8, 8의 왼쪽 7인 트리에서 7을 찾으면 — ' +
        '7&lt;10이라 왼쪽 5로, 7&gt;5라 오른쪽 8로, 7&lt;8이라 왼쪽 7로 이동 → ' +
        '경로 <code>10 → 5 → 8 → 7</code>, 비교 4회.<br>' +
        '<b>삽입 예시.</b> <code>[8, 3, 10, 1, 6, 14]</code>를 차례로 넣으면 8이 루트, 3은 왼쪽, ' +
        '10은 오른쪽, 1은 3의 왼쪽, 6은 8&gt;6→왼쪽 3, 3&lt;6→오른쪽이라 <b>3의 오른쪽 자식</b>이 되고, ' +
        '14는 10의 오른쪽 자식이 된다.',
    },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '아래 <b>메인 시각화</b>에서 값을 직접 삽입해 BST를 만들고, 값을 탐색하면 비교 경로가 강조됩니다. ' +
        '전위/중위/후위/레벨 순회도 애니메이션으로 확인할 수 있습니다.',
    },
    { type: 'viz', component: 'BSTVisualizer' },

    { type: 'h4', text: '5-3. 균형 BST vs 편향 BST — 성능' },
    {
      type: 'p',
      html:
        'BST의 모든 연산 비용은 <b>트리의 높이</b>에 비례한다. 한 번 비교할 때마다 한 레벨씩 내려가기 ' +
        '때문이다. 그래서 <b>같은 데이터라도 모양(높이)에 따라 성능이 극과 극</b>으로 갈린다.',
    },
    {
      type: 'list',
      items: [
        '<b>균형(balanced) BST</b> — 좌우가 고르게 퍼져 높이가 약 <code>log₂ n</code>. 탐색·삽입·삭제 모두 <b>O(log n)</b>.',
        '<b>편향(skewed) BST</b> — <b>정렬된 순서</b>(예: 1,2,3,4,5)로 삽입하면 한쪽으로만 매달려 사실상 연결 리스트가 된다. 높이가 <code>n-1</code>이라 모든 연산이 <b>O(n)</b>으로 악화.',
        '따라서 <b>가장 비효율적인 경우는 "한쪽으로 치우친(편향된) 상태"</b>다.',
      ],
    },
    {
      type: 'table',
      headers: ['연산', '균형 BST', '편향 BST(최악)'],
      rows: [
        ['탐색(search)', 'O(log n)', 'O(n)'],
        ['삽입(insert)', 'O(log n)', 'O(n)'],
        ['삭제(delete)', 'O(log n)', 'O(n)'],
        ['높이(height)', '약 log₂ n', 'n - 1'],
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        'BST는 <b>"모든 경우 O(log n)을 보장하지 않는다."</b> 또한 <b>"항상 O(1) 탐색"도 불가능</b>하다. ' +
        'O(log n)은 <b>균형이 잡혔을 때만</b> 성립하고, 정렬된 입력 등으로 편향되면 O(n)까지 나빠진다. ' +
        '항상 O(log n)을 보장하려면 AVL·레드블랙 트리 같은 <b>자가 균형 BST</b>가 필요하다.',
    },
    { type: 'viz', component: 'BSTShapeViz' },
    {
      type: 'callout',
      tone: 'tip',
      html:
        '위 시각화에서 <b>같은 값들을 다른 삽입 순서</b>로 넣어 보세요. 정렬된 순서로 넣으면 한쪽으로 길게 ' +
        '늘어지고(높이↑, O(n)), 가운데 값부터 골고루 넣으면 균형이 잡혀 높이가 낮아집니다(O(log n)).',
    },

    { type: 'h4', text: '5-4. 삭제(delete) — 세 가지 경우' },
    {
      type: 'p',
      html:
        '삭제는 지운 뒤에도 <b>BST 조건이 유지</b>되어야 해서 가장 까다롭다. 삭제할 노드의 ' +
        '<b>자식 수</b>에 따라 세 경우로 나뉜다.',
    },
    {
      type: 'table',
      headers: ['경우', '상황', '처리 방법'],
      rows: [
        ['① 자식 0개(리프)', '단말 노드 삭제', '그냥 떼어낸다(부모의 해당 자식 링크를 끊음).'],
        ['② 자식 1개', '왼쪽 또는 오른쪽 자식만 있음', '그 하나뿐인 자식(서브트리)을 부모에 그대로 잇는다.'],
        ['③ 자식 2개', '왼쪽·오른쪽 자식 모두 있음', '<b>전임자 또는 후임자</b>로 값을 대체한 뒤, 그 대체 노드를 다시 삭제(가장 복잡).'],
      ],
    },
    {
      type: 'p',
      html:
        '<b>자식이 2개인 노드</b>를 삭제할 때는 그 자리에 들어가도 정렬이 깨지지 않는 값, 즉 ' +
        '<b>바로 직전 값(전임자) 또는 바로 다음 값(후임자)</b>으로 키를 바꿔 끼운다.',
    },
    {
      type: 'list',
      items: [
        '<b>전임자(predecessor)</b> = <b>왼쪽 서브트리의 최댓값</b>. 왼쪽 서브트리에서 오른쪽으로 끝까지 내려가면 나온다.',
        '<b>후임자(successor)</b> = <b>오른쪽 서브트리의 최솟값</b>. 오른쪽 서브트리에서 왼쪽으로 끝까지 내려가면 나온다.',
        '대체값은 자식이 0개 또는 1개인 노드이므로, 값을 옮긴 뒤 ①·② 경우로 다시 간단히 제거한다.',
      ],
    },
    {
      type: 'callout',
      tone: 'warn',
      html:
        'BST 삭제에서 <b>가장 복잡한 경우는 "자식이 2개인 노드"</b>다(자식 0개·1개는 단순 연결 처리). ' +
        '이때 대체값으로는 반드시 <b>왼쪽 서브트리 최댓값(전임자)</b> 또는 <b>오른쪽 서브트리 최솟값(후임자)</b>을 ' +
        '쓴다. "임의 노드와 교환"하거나 "전체 재정렬"하는 것이 아니다.',
    },

    // ───────────────────────────────────────────── 6. 한눈에 정리
    { type: 'h3', text: '6. 한눈에 정리' },
    {
      type: 'table',
      headers: ['주제', '핵심', '자주 틀리는 점'],
      rows: [
        ['트리 용어', '루트는 1개·부모 없음 / 높이 = 가장 긴 경로 길이', '루트도 부모가 있다(X), 높이=노드 수(X)'],
        ['순회 분류', '전위·중위·후위 = DFS / 레벨 = BFS', '레벨 순회를 DFS로 착각'],
        ['순회 순서', '전위 NLR · 중위 LNR · 후위 LRN', 'N(현재 노드)을 찍는 위치만 다름'],
        ['완전 이진 트리', '마지막 레벨 왼쪽부터 / 배열 표현', '마지막 레벨도 꽉 차야 한다(X)'],
        ['배열 인덱스', '자식 2i+1·2i+2 / 부모 (i-1)//2', '1-기반(2i·2i+1)과 혼동'],
        ['BST 조건', '왼쪽<부모<오른쪽 (서브트리 전체)', '직속 자식만 비교(X)'],
        ['BST 중위', '오름차순 정렬 결과', '중위로 루트를 특정 가능(X)'],
        ['BST 성능', '균형 O(log n) / 편향 O(n)', '항상 O(log n) 보장(X), O(1)(X)'],
        ['BST 삭제', '자식 2개가 최악 / 전임자·후임자로 대체', '임의 교환·전체 재정렬(X)'],
      ],
    },
  ],
}
