// Ch09 트리 — 세부 학습 페이지 (5개 주제)
// 1) terms 2) traversal 3) binary 4) bst 5) bstdel

export const subtopics = [
  // ──────────────────────────────────────────────────────────
  // 1) 트리 용어와 종류
  // ──────────────────────────────────────────────────────────
  {
    slug: 'terms',
    title: '트리 용어와 종류',
    summary:
      '트리(tree)는 계층적 관계를 표현하는 비선형 자료구조다. 하나의 루트(root)에서 시작해 ' +
      '부모-자식(parent-child) 관계로 가지를 뻗으며, 사이클이 없는 연결 구조다. 노드 n개인 트리는 항상 ' +
      '간선이 n-1개다. 이 페이지에서는 루트·부모·자식·형제·리프·차수·레벨·높이·서브트리 같은 핵심 용어를 ' +
      '정확히 정의하고, 특히 자주 헷갈리는 "높이는 노드 개수가 아니라 가장 긴 경로의 길이"라는 점을 짚는다.',
    conceptTags: ['트리 용어'],
    blocks: [
      { type: 'h3', text: '트리란 무엇인가' },
      {
        type: 'p',
        html:
          '<b>트리</b>는 노드(node)와 그 사이를 잇는 간선(edge)으로 이루어진 비선형 자료구조다. ' +
          '배열·연결 리스트가 데이터를 한 줄로 늘어놓는 <b>선형(linear)</b> 구조라면, 트리는 데이터를 ' +
          '<b>계층(hierarchy)</b>으로 표현한다. 폴더-하위폴더, 조직도, HTML DOM 트리, 파일 시스템이 모두 ' +
          '트리 구조다.',
      },
      {
        type: 'list',
        items: [
          '<b>루트(root)</b>가 정확히 하나 존재하며, 부모가 없는 유일한 최상위 노드다.',
          '루트를 제외한 모든 노드는 <b>부모가 정확히 하나</b>다.',
          '<b>사이클(cycle)이 없다.</b> 어떤 노드에서 출발해 자기 자신으로 돌아오는 경로가 없다.',
          '노드가 <code>n</code>개이면 간선은 항상 <code>n-1</code>개다. (루트만 부모 간선이 없으므로)',
        ],
      },
      {
        type: 'callout',
        tone: 'warn',
        html:
          '"모든 노드는 반드시 부모를 가진다"는 <b>틀린</b> 설명이다. 루트는 부모가 없다. ' +
          '이 점이 객관식·O/X에서 자주 함정으로 나온다.',
      },
      { type: 'h3', text: '핵심 용어 정리' },
      {
        type: 'table',
        headers: ['용어', '정의', '예시 / 보충'],
        rows: [
          ['루트(root)', '부모가 없는 단 하나의 최상위 노드', '트리의 시작점. 하나만 존재'],
          ['부모(parent)', '어떤 노드 바로 위에서 그 노드를 자식으로 가지는 노드', '루트 외 모든 노드는 부모가 1개'],
          ['자식(child)', '어떤 노드 바로 아래에 직접 연결된 노드', '한 노드는 0개 이상 자식을 가짐'],
          ['형제(sibling)', '부모가 같은 노드들', '같은 부모를 공유'],
          ['리프(leaf)', '자식이 없는 노드 (= 단말 노드, 외부 노드)', '차수가 0인 노드'],
          ['내부 노드(internal)', '자식이 하나 이상 있는 노드', '리프가 아닌 노드'],
          ['차수(degree)', '한 노드가 가진 자식의 수', '리프의 차수는 0'],
          ['레벨(level)', '루트로부터 떨어진 단계. 루트를 레벨 0(또는 1)로 셈', '같은 레벨 = 루트에서 같은 거리'],
          ['깊이(depth)', '루트에서 그 노드까지의 경로 길이', '루트의 깊이는 0'],
          ['높이(height)', '한 노드에서 가장 먼 리프까지의 경로 길이(간선 수)', '트리의 높이 = 루트의 높이'],
          ['서브트리(subtree)', '어떤 노드와 그 자손 전체가 이루는 트리', '재귀적 정의의 핵심'],
          ['조상/자손', '위쪽 경로상의 노드 = 조상, 아래쪽 = 자손', '부모·자식의 일반화'],
        ],
      },
      { type: 'h3', text: '높이(height)를 정확히' },
      {
        type: 'p',
        html:
          '가장 많이 틀리는 개념이 <b>높이</b>다. 높이는 <b>전체 노드의 개수가 아니다.</b> ' +
          '높이는 <b>가장 긴 경로의 길이</b>, 즉 루트에서 가장 먼 리프까지 거쳐야 하는 <b>간선의 수</b>다. ' +
          '예를 들어 루트 하나뿐인 트리의 높이는 0이고, 루트-자식-손자로 한 줄로 이어진 트리의 높이는 2다 ' +
          '(노드는 3개지만 높이는 2).',
      },
      {
        type: 'callout',
        tone: 'tip',
        html:
          '교재에 따라 높이를 "노드 수"로 세어 +1 차이가 나기도 한다. 본 위키는 일관되게 <b>간선 수(경로 길이)</b> ' +
          '기준을 사용한다. 핵심은 "노드 총개수와는 다르다"는 점이다.',
      },
      { type: 'h3', text: '트리의 종류' },
      {
        type: 'list',
        items: [
          '<b>일반 트리</b> — 한 노드가 자식을 몇 개든 가질 수 있다.',
          '<b>이진 트리(binary tree)</b> — 각 노드가 최대 2개의 자식(왼쪽/오른쪽)을 가진다.',
          '<b>완전 이진 트리(complete binary tree)</b> — 마지막 레벨을 제외하고 꽉 차 있고, 마지막 레벨은 왼쪽부터 채운다.',
          '<b>이진 검색 트리(BST)</b> — 왼쪽 &lt; 부모 &lt; 오른쪽 규칙을 만족하는 이진 트리.',
        ],
      },
      {
        type: 'p',
        html:
          '이 단원의 나머지 페이지에서 <b>순회</b>(전위·중위·후위·레벨), <b>이진 트리와 배열 표현</b>, ' +
          '<b>BST의 탐색·삽입·삭제</b>를 차례로 깊게 다룬다.',
      },
    ],
  },

  // ──────────────────────────────────────────────────────────
  // 2) 트리 순회
  // ──────────────────────────────────────────────────────────
  {
    slug: 'traversal',
    title: '트리 순회',
    summary:
      '순회(traversal)는 트리의 모든 노드를 빠짐없이 한 번씩 방문하는 방법이다. 깊이 우선(DFS)에는 ' +
      '방문 시점에 따라 전위(preorder)·중위(inorder)·후위(postorder) 세 가지가 있고, 너비 우선(BFS)에는 ' +
      '레벨 순회(level order)가 있다. 특히 BST를 중위 순회하면 키가 항상 오름차순으로 나온다는 성질이 핵심이다.',
    conceptTags: ['순회', 'DFS/BFS'],
    blocks: [
      { type: 'h3', text: '핵심 아이디어: 노드를 언제 방문하는가' },
      {
        type: 'p',
        html:
          '이진 트리의 DFS 순회는 한 노드에서 <b>왼쪽 서브트리·자기 자신·오른쪽 서브트리</b> 세 가지 일을 ' +
          '재귀적으로 처리한다. 이때 <b>자기 자신(노드)을 처리하는 시점</b>이 언제냐에 따라 세 가지로 나뉜다.',
      },
      {
        type: 'list',
        items: [
          '<b>전위(preorder)</b> — 노드 → 왼쪽 → 오른쪽. (노드를 가장 먼저)',
          '<b>중위(inorder)</b> — 왼쪽 → 노드 → 오른쪽. (노드를 가운데)',
          '<b>후위(postorder)</b> — 왼쪽 → 오른쪽 → 노드. (노드를 가장 마지막)',
        ],
      },
      {
        type: 'p',
        html:
          '이 셋은 모두 한 경로를 리프까지 깊이 내려갔다 되돌아오는 <b>DFS(깊이 우선 탐색)</b>다. ' +
          '반면 <b>레벨 순회(level order)</b>는 같은 레벨을 모두 방문한 뒤 다음 레벨로 내려가는 ' +
          '<b>BFS(너비 우선 탐색)</b>로, 큐(queue)를 사용해 구현한다.',
      },
      { type: 'h3', text: '분류 한눈에 보기' },
      {
        type: 'table',
        headers: ['순회', '방문 순서', '분류', '대표 용도'],
        rows: [
          ['전위(preorder)', '노드 → 왼쪽 → 오른쪽', 'DFS', '트리 복제, 식의 prefix 표기, 구조 직렬화'],
          ['중위(inorder)', '왼쪽 → 노드 → 오른쪽', 'DFS', 'BST를 오름차순으로 출력'],
          ['후위(postorder)', '왼쪽 → 오른쪽 → 노드', 'DFS', '자식 먼저 처리(메모리 해제, 디렉터리 용량 합산)'],
          ['레벨(level order)', '위 레벨부터 좌 → 우', 'BFS', '레벨별 처리, 최단 거리, 너비 측정'],
        ],
      },
      { type: 'h3', text: '동작 과정 (예시 트리)' },
      {
        type: 'p',
        html:
          '루트 <code>8</code>, 왼쪽 자식 <code>3</code>(자식 <code>1</code>, <code>6</code>), ' +
          '오른쪽 자식 <code>10</code>(오른쪽 자식 <code>14</code>)인 BST를 생각하자.',
      },
      {
        type: 'list',
        items: [
          '<b>전위</b>: 8 → 3 → 1 → 6 → 10 → 14',
          '<b>중위</b>: 1 → 3 → 6 → 8 → 10 → 14 &nbsp;(오름차순! BST이기 때문)',
          '<b>후위</b>: 1 → 6 → 3 → 14 → 10 → 8',
          '<b>레벨</b>: 8 → 3 → 10 → 1 → 6 → 14',
        ],
      },
      { type: 'h3', text: '참조 구현' },
      {
        type: 'code',
        code:
          'from collections import deque\n' +
          '\n' +
          'class Node:\n' +
          '    def __init__(self, key):\n' +
          '        self.key = key\n' +
          '        self.left = None\n' +
          '        self.right = None\n' +
          '\n' +
          '# ── DFS: 재귀 순회 ──\n' +
          'def preorder(node, out):\n' +
          '    if node is None:\n' +
          '        return\n' +
          '    out.append(node.key)      # 노드\n' +
          '    preorder(node.left, out)  # 왼쪽\n' +
          '    preorder(node.right, out) # 오른쪽\n' +
          '\n' +
          'def inorder(node, out):\n' +
          '    if node is None:\n' +
          '        return\n' +
          '    inorder(node.left, out)   # 왼쪽\n' +
          '    out.append(node.key)      # 노드\n' +
          '    inorder(node.right, out)  # 오른쪽\n' +
          '\n' +
          'def postorder(node, out):\n' +
          '    if node is None:\n' +
          '        return\n' +
          '    postorder(node.left, out)  # 왼쪽\n' +
          '    postorder(node.right, out) # 오른쪽\n' +
          '    out.append(node.key)       # 노드\n' +
          '\n' +
          '# ── BFS: 레벨 순회 (큐) ──\n' +
          'def level_order(root):\n' +
          '    out = []\n' +
          '    if root is None:\n' +
          '        return out\n' +
          '    q = deque([root])\n' +
          '    while q:\n' +
          '        node = q.popleft()\n' +
          '        out.append(node.key)\n' +
          '        if node.left:\n' +
          '            q.append(node.left)\n' +
          '        if node.right:\n' +
          '            q.append(node.right)\n' +
          '    return out',
        caption: '전위·중위·후위(DFS 재귀)와 레벨 순회(BFS, 큐) 구현',
      },
      { type: 'h3', text: '시간·공간 복잡도' },
      {
        type: 'table',
        headers: ['순회', '시간복잡도', '공간복잡도', '비고'],
        rows: [
          ['전위/중위/후위', 'O(n)', 'O(h)', '재귀 호출 스택 깊이 = 트리 높이 h'],
          ['레벨 순회', 'O(n)', 'O(w)', '큐에 한 레벨 최대 너비 w개 보관'],
        ],
      },
      {
        type: 'callout',
        tone: 'tip',
        html:
          '<b>BST 중위 순회 = 오름차순 정렬</b>은 시험·실무 단골이다. 거꾸로 노드 → 오른쪽 → 왼쪽이 아니라 ' +
          '왼쪽을 먼저 가는 점, 그리고 이 성질이 "BST일 때만" 성립한다는 점을 기억하자. 일반 이진 트리의 ' +
          '중위 순회는 정렬과 무관하다.',
      },
      {
        type: 'callout',
        tone: 'warn',
        html:
          '치우친(skewed) 트리에서는 재귀 깊이가 n까지 커져 파이썬 기본 재귀 한도를 넘을 수 있다. ' +
          '실무에서는 명시적 스택을 쓰는 반복형 순회나 재귀 한도 조정이 필요할 수 있다.',
      },
      { type: 'viz', component: 'TraversalCompareViz' },
    ],
  },

  // ──────────────────────────────────────────────────────────
  // 3) 이진 트리와 배열 표현
  // ──────────────────────────────────────────────────────────
  {
    slug: 'binary',
    title: '이진 트리와 배열 표현',
    summary:
      '이진 트리(binary tree)는 각 노드가 최대 2개의 자식(왼쪽·오른쪽)을 가지는 트리다. 그중 마지막 레벨을 ' +
      '제외하고 모두 꽉 차 있고 마지막 레벨은 왼쪽부터 채운 것이 완전 이진 트리(complete binary tree)다. ' +
      '완전 이진 트리는 포인터 없이 배열만으로 깔끔하게 표현할 수 있다 — 0-기반 인덱스에서 왼쪽 자식 2i+1, ' +
      '오른쪽 자식 2i+2, 부모 (i-1)//2. 힙(heap)이 바로 이 표현을 사용한다.',
    conceptTags: ['이진 트리', '완전 이진 트리', '배열 표현'],
    blocks: [
      { type: 'h3', text: '이진 트리' },
      {
        type: 'p',
        html:
          '<b>이진 트리</b>는 각 노드가 <b>최대 2개</b>의 자식을 가지는 트리다. 두 자식은 순서가 있어 ' +
          '각각 <b>왼쪽 자식·오른쪽 자식</b>으로 구분한다. 자식이 0개·1개여도 이진 트리다(최대가 2라는 의미).',
      },
      {
        type: 'list',
        items: [
          '<b>정 이진 트리(full)</b> — 모든 노드가 자식을 0개 또는 2개 가진다(1개인 노드 없음).',
          '<b>포화 이진 트리(perfect)</b> — 모든 내부 노드가 2개 자식이고 모든 리프가 같은 레벨. 완전히 꽉 찬 형태.',
          '<b>완전 이진 트리(complete)</b> — 마지막 레벨 빼고 꽉 차 있고, 마지막 레벨은 왼쪽부터 채운다.',
        ],
      },
      { type: 'h3', text: '완전 이진 트리' },
      {
        type: 'p',
        html:
          '<b>완전 이진 트리</b>는 위 레벨부터 빈틈없이 채우되 마지막 레벨만 <b>왼쪽부터 차례로</b> 채운다. ' +
          '마지막 레벨이 꽉 찰 필요는 없지만, 중간에 빈 자리를 두고 오른쪽을 먼저 채우면 안 된다. ' +
          '이 "빈틈없이 왼쪽부터" 규칙 덕분에 노드들을 <b>레벨 순서대로 배열에 1:1로 담을 수</b> 있다.',
      },
      {
        type: 'callout',
        tone: 'warn',
        html:
          '"완전 이진 트리는 마지막 레벨까지 반드시 꽉 차 있어야 한다"는 <b>틀린</b> 설명이다. ' +
          '마지막 레벨까지 완전히 꽉 찬 것은 <b>포화(perfect) 이진 트리</b>다. 완전 이진 트리는 ' +
          '마지막 레벨이 왼쪽부터 채워지기만 하면 된다.',
      },
      { type: 'h3', text: '배열 표현 (0-기반 인덱스)' },
      {
        type: 'p',
        html:
          '완전 이진 트리를 레벨 순회 순서대로 배열 <code>A[0..n-1]</code>에 담으면, 부모-자식 관계를 ' +
          '<b>인덱스 산술</b>만으로 계산할 수 있어 포인터가 필요 없다. 인덱스 <code>i</code>인 노드에 대해:',
      },
      {
        type: 'table',
        headers: ['관계', '인덱스 공식 (0-기반)', '설명'],
        rows: [
          ['왼쪽 자식', '2i + 1', 'i의 왼쪽 자식 위치'],
          ['오른쪽 자식', '2i + 2', 'i의 오른쪽 자식 위치'],
          ['부모', '(i - 1) // 2', '정수 나눗셈(내림)'],
          ['루트', 'A[0]', '인덱스 0이 루트'],
          ['리프 시작', 'A[n//2 .. n-1]', '뒤쪽 절반은 자식이 없는 노드'],
        ],
      },
      {
        type: 'p',
        html:
          '예를 들어 <code>A = [8, 3, 10, 1, 6, 14]</code>이면, 루트는 8(인덱스 0), 그 왼쪽 자식은 ' +
          '<code>A[2·0+1] = A[1] = 3</code>, 오른쪽 자식은 <code>A[2·0+2] = A[2] = 10</code>이다. ' +
          '노드 3(인덱스 1)의 부모는 <code>A[(1-1)//2] = A[0] = 8</code>로 정확히 복원된다.',
      },
      { type: 'h3', text: '참조 구현' },
      {
        type: 'code',
        code:
          '# 완전 이진 트리의 배열 표현 헬퍼 (0-기반 인덱스)\n' +
          'def left(i):   # 왼쪽 자식 인덱스\n' +
          '    return 2 * i + 1\n' +
          '\n' +
          'def right(i):  # 오른쪽 자식 인덱스\n' +
          '    return 2 * i + 2\n' +
          '\n' +
          'def parent(i): # 부모 인덱스 (정수 나눗셈)\n' +
          '    return (i - 1) // 2\n' +
          '\n' +
          'def children(arr, i):\n' +
          '    n = len(arr)\n' +
          '    res = []\n' +
          '    l, r = left(i), right(i)\n' +
          '    if l < n:\n' +
          '        res.append(arr[l])\n' +
          '    if r < n:\n' +
          '        res.append(arr[r])\n' +
          '    return res\n' +
          '\n' +
          '# 배열 = 레벨 순회 결과. 인덱스만으로 트리 탐색 가능\n' +
          'A = [8, 3, 10, 1, 6, 14]\n' +
          'print(A[left(0)], A[right(0)])      # 3 10  (루트의 두 자식)\n' +
          'print(A[parent(4)])                 # 6의 부모 = 3 (인덱스 4 -> 부모 인덱스 1)',
        caption: '배열로 표현한 완전 이진 트리의 부모/자식 인덱스 계산',
      },
      { type: 'h3', text: '배열 표현의 장단점' },
      {
        type: 'table',
        headers: ['항목', '내용'],
        rows: [
          ['장점', '포인터 불필요, 캐시 친화적(연속 메모리), 인덱스 산술로 O(1) 부모/자식 접근'],
          ['단점', '완전 이진 트리가 아니면 빈칸이 생겨 메모리 낭비. 일반/치우친 트리에는 부적합'],
          ['대표 사용처', '힙(heap) — 우선순위 큐, 힙 정렬의 기반 구조'],
        ],
      },
      {
        type: 'callout',
        tone: 'tip',
        html:
          '힙은 항상 완전 이진 트리 모양을 유지하기 때문에 배열 표현이 딱 들어맞는다. 아래 시각화에서 ' +
          '트리 모양과 배열 인덱스가 어떻게 대응되는지 직접 확인해 보세요.',
      },
      { type: 'viz', component: 'HeapArrayViz' },
    ],
  },

  // ──────────────────────────────────────────────────────────
  // 4) 이진 검색 트리(BST)
  // ──────────────────────────────────────────────────────────
  {
    slug: 'bst',
    title: '이진 검색 트리(BST)',
    summary:
      '이진 검색 트리(Binary Search Tree)는 모든 노드에서 "왼쪽 서브트리 < 부모 < 오른쪽 서브트리" 규칙이 ' +
      '성립하는 이진 트리다. 이 정렬 불변식 덕분에 탐색·삽입은 루트에서 비교 결과에 따라 한 방향으로만 ' +
      '내려가 O(h)에 끝난다. 균형이 잡히면 h ≈ log n이라 O(log n)이지만, 정렬된 순서로 삽입하면 한쪽으로 ' +
      '치우쳐 리스트처럼 되어 O(n)까지 나빠진다. 중위 순회하면 항상 오름차순이 나온다.',
    conceptTags: ['BST', 'BST 탐색', 'BST 삽입'],
    blocks: [
      { type: 'h3', text: 'BST의 정렬 불변식' },
      {
        type: 'p',
        html:
          'BST는 모든 노드에서 <b>왼쪽 서브트리의 모든 키 &lt; 그 노드의 키 &lt; 오른쪽 서브트리의 모든 키</b>가 ' +
          '성립한다. 즉 어떤 노드를 기준으로 작은 값은 전부 왼쪽, 큰 값은 전부 오른쪽에 모인다. ' +
          '이 불변식이 "한 번 비교하면 후보의 절반을 버릴 수 있는" 이진 탐색의 트리 버전을 가능하게 한다.',
      },
      {
        type: 'callout',
        tone: 'warn',
        html:
          '불변식은 <b>직계 자식뿐 아니라 서브트리 전체</b>에 적용된다. 왼쪽 자식 하나만 부모보다 작은 게 아니라, ' +
          '왼쪽 서브트리에 속한 <b>모든</b> 노드가 부모보다 작아야 한다. (보통 중복 키는 허용하지 않는다고 가정)',
      },
      { type: 'h3', text: '탐색(search)의 동작 과정' },
      {
        type: 'list',
        items: [
          '루트에서 시작해 찾는 값 <code>key</code>와 현재 노드를 비교한다.',
          '같으면 찾았으므로 반환한다.',
          '<code>key</code>가 더 작으면 <b>왼쪽 서브트리</b>로만 내려간다.',
          '<code>key</code>가 더 크면 <b>오른쪽 서브트리</b>로만 내려간다.',
          '내려갈 자식이 없으면(None) 트리에 없는 값이다.',
        ],
      },
      {
        type: 'p',
        html:
          '핵심은 <b>비교 결과에 따라 한쪽 서브트리만</b> 따라간다는 것이다. 양쪽을 다 보지 않으므로 ' +
          '한 번 내려갈 때마다 후보가 줄고, 방문 노드 수는 트리 높이 <code>h</code>에 비례한다 → <b>O(h)</b>.',
      },
      { type: 'h3', text: '삽입(insert)의 동작 과정' },
      {
        type: 'list',
        items: [
          '탐색과 똑같이 루트에서 비교하며 내려간다.',
          '내려가다 자식 자리가 비어 있으면(None) <b>바로 그 자리</b>에 새 노드를 단다.',
          '항상 <b>리프 자리</b>에 추가되며, 기존 노드의 값은 바꾸지 않는다.',
          '삽입 위치는 "정렬 조건을 만족하는 유일한 자리"로 결정된다.',
        ],
      },
      {
        type: 'p',
        html:
          '예: 빈 트리에 <code>[8, 3, 10, 1, 6, 14]</code>를 차례로 삽입하면 8이 루트가 되고, 3은 8의 왼쪽, ' +
          '10은 8의 오른쪽, 1은 3의 왼쪽, 6은 3의 오른쪽, 14는 10의 오른쪽에 달린다. ' +
          '<b>가장 먼저 삽입한 값이 루트</b>가 된다는 점에 주의.',
      },
      { type: 'h3', text: '참조 구현' },
      {
        type: 'code',
        code:
          'class Node:\n' +
          '    def __init__(self, key):\n' +
          '        self.key = key\n' +
          '        self.left = None\n' +
          '        self.right = None\n' +
          '\n' +
          'def insert(root, key):\n' +
          '    """BST 조건을 만족하는 위치를 찾아 새 노드를 단다. 새 루트를 반환."""\n' +
          '    if root is None:\n' +
          '        return Node(key)\n' +
          '    if key < root.key:\n' +
          '        root.left = insert(root.left, key)   # 왼쪽으로\n' +
          '    elif key > root.key:\n' +
          '        root.right = insert(root.right, key) # 오른쪽으로\n' +
          '    # key == root.key 이면 중복: 그대로 둠\n' +
          '    return root\n' +
          '\n' +
          'def search(root, key):\n' +
          '    """비교 결과에 따라 한 방향으로만 내려간다. O(h)."""\n' +
          '    node = root\n' +
          '    while node is not None:\n' +
          '        if key == node.key:\n' +
          '            return node          # 찾음\n' +
          '        node = node.left if key < node.key else node.right\n' +
          '    return None                  # 없음\n' +
          '\n' +
          'def inorder(node, out):\n' +
          '    """중위 순회 = 오름차순."""\n' +
          '    if node is None:\n' +
          '        return\n' +
          '    inorder(node.left, out)\n' +
          '    out.append(node.key)\n' +
          '    inorder(node.right, out)\n' +
          '\n' +
          '# 사용 예\n' +
          'root = None\n' +
          'for x in [8, 3, 10, 1, 6, 14]:\n' +
          '    root = insert(root, x)\n' +
          'res = []\n' +
          'inorder(root, res)\n' +
          'print(res)                       # [1, 3, 6, 8, 10, 14]  오름차순',
        caption: 'BST 노드 클래스와 insert / search / inorder(정렬 출력) 구현',
      },
      { type: 'h3', text: '시간복잡도' },
      {
        type: 'table',
        headers: ['연산', '평균(균형)', '최악(편향)', '설명'],
        rows: [
          ['탐색(search)', 'O(log n)', 'O(n)', '높이 h에 비례, h는 균형 시 log n'],
          ['삽입(insert)', 'O(log n)', 'O(n)', '탐색으로 자리 찾은 뒤 리프에 연결'],
          ['중위 순회', 'O(n)', 'O(n)', '모든 노드 방문, 결과는 정렬됨'],
          ['공간', 'O(n)', 'O(n)', '노드 n개 저장'],
        ],
      },
      {
        type: 'p',
        html:
          'BST 연산은 모두 <b>높이 h</b>에 의해 결정된다. 균형이 잘 잡힌 BST는 <code>h ≈ log₂ n</code>이라 ' +
          '탐색·삽입이 <b>O(log n)</b>으로 빠르다. 하지만 <b>정렬된 순서</b>(예: 1, 2, 3, 4, 5)로 삽입하면 ' +
          '모든 노드가 한쪽으로만 달려 <b>높이가 n</b>이 되고, 사실상 연결 리스트와 같아져 <b>O(n)</b>으로 ' +
          '나빠진다.',
      },
      {
        type: 'callout',
        tone: 'warn',
        html:
          '<b>편향(skewed) 트리</b>가 BST 최악의 적이다. 입력이 이미 정렬되어 있으면 일반 BST는 망가진다. ' +
          '이를 피하려고 <b>AVL 트리·레드블랙 트리</b> 같은 <b>자가 균형 BST</b>가 삽입/삭제 때 회전으로 ' +
          '높이를 항상 O(log n)으로 유지한다.',
      },
      {
        type: 'callout',
        tone: 'tip',
        html:
          'BST의 장점: ① 중위 순회로 정렬 결과를 공짜로 얻는다, ② 동적 삽입/삭제에 강하다(배열 이진 탐색은 삽입이 ' +
          'O(n)). 단점: 균형이 깨지면 O(n)으로 추락. "항상 O(log n) 보장"은 일반 BST에 대해 <b>틀린</b> 설명이다.',
      },
      { type: 'viz', component: 'BSTVisualizer' },
      { type: 'viz', component: 'BSTShapeViz' },
    ],
  },

  // ──────────────────────────────────────────────────────────
  // 5) BST 삭제
  // ──────────────────────────────────────────────────────────
  {
    slug: 'bstdel',
    title: 'BST 삭제',
    summary:
      'BST에서 노드를 삭제하면 "왼쪽 < 부모 < 오른쪽" 불변식이 깨지지 않게 트리를 재구성해야 한다. 삭제할 ' +
      '노드의 자식 수에 따라 세 경우로 나뉜다 — 자식 0개(그냥 제거), 자식 1개(자식으로 대체), 자식 2개(가장 ' +
      '복잡: 왼쪽 서브트리의 최댓값=전임자 또는 오른쪽 서브트리의 최솟값=후임자로 값을 대체한 뒤 그 대체 노드를 ' +
      '삭제). 대체값이 전임자/후임자여야 정렬 불변식이 유지된다.',
    conceptTags: ['BST 삭제'],
    blocks: [
      { type: 'h3', text: '삭제가 까다로운 이유' },
      {
        type: 'p',
        html:
          '삽입은 항상 리프에 노드를 다는 단순한 일이지만, 삭제는 <b>트리 중간의 노드</b>를 빼낼 수 있다. ' +
          '중간 노드를 그냥 없애면 그 자리에 구멍이 생겨 자식들이 트리에서 떨어져 나간다. 따라서 ' +
          '<b>정렬 불변식을 유지하면서 구멍을 메우는</b> 방법이 자식 수에 따라 달라진다.',
      },
      { type: 'h3', text: '경우 1 — 자식이 0개 (리프)' },
      {
        type: 'p',
        html:
          '가장 간단하다. 그 노드를 부모와의 연결에서 끊어 <b>그냥 제거</b>한다(부모의 해당 자식 포인터를 None으로). ' +
          '트리의 나머지는 그대로 유효하다.',
      },
      { type: 'h3', text: '경우 2 — 자식이 1개' },
      {
        type: 'p',
        html:
          '삭제할 노드를 빼고, 그 <b>유일한 자식(서브트리)을 삭제 노드 자리로 끌어올린다.</b> ' +
          '한 자식 서브트리 전체가 부모보다 작거나(왼쪽) 크다(오른쪽)는 성질은 그대로 유지되므로 ' +
          '대체해도 불변식이 깨지지 않는다.',
      },
      { type: 'h3', text: '경우 3 — 자식이 2개 (가장 복잡)' },
      {
        type: 'p',
        html:
          '두 자식을 모두 가진 노드는 단순히 한쪽으로 끌어올릴 수 없다. 대신 그 노드의 <b>값만</b> 정렬 순서상 ' +
          '바로 이웃한 값으로 바꾼 뒤, 원래 그 이웃 값이 있던(자식이 1개 이하인) 자리를 삭제한다. 이웃 값으로는 ' +
          '둘 중 하나를 쓴다:',
      },
      {
        type: 'list',
        items: [
          '<b>전임자(predecessor)</b> = <b>왼쪽 서브트리의 최댓값</b>. (왼쪽으로 한 번 간 뒤 계속 오른쪽 끝까지)',
          '<b>후임자(successor)</b> = <b>오른쪽 서브트리의 최솟값</b>. (오른쪽으로 한 번 간 뒤 계속 왼쪽 끝까지)',
        ],
      },
      {
        type: 'p',
        html:
          '전임자/후임자는 정렬 순서에서 삭제할 값의 바로 양옆 값이라, 그 값으로 대체하면 ' +
          '"왼쪽 &lt; 부모 &lt; 오른쪽"이 그대로 유지된다. 게다가 전임자(오른쪽 끝)·후임자(왼쪽 끝)는 ' +
          '<b>자식이 많아야 한 개</b>이므로, 그 노드를 실제로 지우는 일은 경우 1 또는 경우 2로 환원된다.',
      },
      {
        type: 'callout',
        tone: 'warn',
        html:
          '임의의 노드와 그냥 바꾸면 안 된다. 반드시 <b>전임자(왼쪽 서브트리 최댓값)</b> 또는 ' +
          '<b>후임자(오른쪽 서브트리 최솟값)</b>여야 정렬 불변식이 보존된다.',
      },
      { type: 'h3', text: '동작 과정 요약' },
      {
        type: 'table',
        headers: ['자식 수', '처리 방법', '불변식 유지 근거'],
        rows: [
          ['0개', '부모와의 연결을 끊고 제거', '리프 제거는 나머지에 영향 없음'],
          ['1개', '유일한 자식 서브트리를 자리로 끌어올림', '서브트리 전체가 한쪽 범위를 만족'],
          ['2개', '후임자(또는 전임자) 값으로 대체 후 그 노드 삭제', '대체값이 정렬상 바로 이웃이라 순서 보존'],
        ],
      },
      { type: 'h3', text: '참조 구현' },
      {
        type: 'code',
        code:
          'class Node:\n' +
          '    def __init__(self, key):\n' +
          '        self.key = key\n' +
          '        self.left = None\n' +
          '        self.right = None\n' +
          '\n' +
          'def _min_node(node):\n' +
          '    """서브트리의 최솟값 노드 = 후임자 후보 (왼쪽 끝)."""\n' +
          '    while node.left is not None:\n' +
          '        node = node.left\n' +
          '    return node\n' +
          '\n' +
          'def delete(root, key):\n' +
          '    """key 노드를 삭제하고 새 루트를 반환한다."""\n' +
          '    if root is None:\n' +
          '        return None\n' +
          '\n' +
          '    if key < root.key:\n' +
          '        root.left = delete(root.left, key)\n' +
          '    elif key > root.key:\n' +
          '        root.right = delete(root.right, key)\n' +
          '    else:\n' +
          '        # 찾은 노드: 자식 수로 분기\n' +
          '        if root.left is None:        # 경우1/2: 오른쪽 자식(또는 None)으로 대체\n' +
          '            return root.right\n' +
          '        if root.right is None:       # 경우2: 왼쪽 자식으로 대체\n' +
          '            return root.left\n' +
          '        # 경우3: 자식 2개 -> 오른쪽 서브트리 최솟값(후임자)으로 대체\n' +
          '        succ = _min_node(root.right)\n' +
          '        root.key = succ.key                       # 값만 복사\n' +
          '        root.right = delete(root.right, succ.key)  # 그 후임자 노드를 삭제\n' +
          '    return root',
        caption: 'BST 삭제: 자식 0/1/2개 세 경우를 후임자 대체로 처리',
      },
      { type: 'h3', text: '시간복잡도' },
      {
        type: 'table',
        headers: ['연산', '평균(균형)', '최악(편향)', '설명'],
        rows: [
          ['삭제(delete)', 'O(log n)', 'O(n)', '노드 탐색 O(h) + 후임자 탐색 O(h)'],
        ],
      },
      {
        type: 'p',
        html:
          '삭제도 결국 트리 높이 <code>h</code>에 비례한다. 삭제 노드를 찾는 데 O(h), 자식 2개일 때 후임자를 ' +
          '찾는 데 다시 O(h)가 들지만 합쳐도 O(h)다. 균형 트리면 O(log n), 편향 트리면 O(n)이다.',
      },
      {
        type: 'callout',
        tone: 'tip',
        html:
          '구현에서는 후임자(오른쪽 최솟값) 또는 전임자(왼쪽 최댓값) 중 무엇을 써도 정답이다. 위 코드는 ' +
          '후임자를 사용했고, "값만 복사하고 그 후임자 노드를 재귀로 삭제"하는 패턴이 자식 2개 경우를 ' +
          '자식 1개 이하 경우로 깔끔하게 환원한다. 아래 시각화로 세 경우를 직접 삭제해 보세요.',
      },
      { type: 'viz', component: 'BSTVisualizer' },
    ],
  },
]
